# Changelog Graph Join-Fragment Leak

**Goal:** Remove literal `, ].join('')` text and stray `[` chars leaking into the `/ds/#/changelog` activity graph.
**Architecture:** Pure template repair in `changelog/graph.js`; no structural change, week/day markup identical.
**Tech Stack:** Vanilla JS ESM, Vite build, lint-manage + lint:tokens gates.
**Tags:** Component

<!-- changelog: hide -->

---

## Phase 1 — Graph template repair {#phase-1}

*Cut the stray join fragments left by a botched width-wrap; verify graph renders clean.*

*Tags: Component*

| # | Task | Done when |
|---|------|-----------|
| 1 | Remove stray fragments in cells/weeks | `graph.js` ≤100 lines, no `].join` literal inside templates, build green |
| 2 | Regression + browser verify | lint-manage 0, npm test green, graph + drawer cycle pass on :5199 |
| 3 | Hide test scope from changelog | leak card gone, count back to 13, gates green |
| 4 | Hide split scope from changelog | splits card gone, gates green |

### Task 1: Remove stray fragments in cells/weeks ✓ done

**Objective:** Restore the cells array tail and weeks return to plain template joins, byte-identical HTML output minus the leak.
**Files:** src/ds/changelog/graph.js.
**Verify:** `node --check` clean; rendered graph contains no `join` text; `npm test` green.

### Task 2: Regression + browser verify ✓ done

**Objective:** Full gates plus real-browser pass: graph cells render, day filter toggles, drawer cycle intact.
**Files:** .hermes/plans/2026-09-14_001100-graph-join-leak.md, src/ds/changelog-manifest.json.
**Verify:** `node scripts/lint-manage.mjs` clean; `npm test` green; :5199 graph screenshot clean.

### Task 3: Hide test scope from changelog ✓ done

**Objective:** Keep this build-testing scope out of the changelog cards via `<!-- changelog: hide -->` + HIDDEN filter in the composer.
**Files:** src/ds/pages-changelog.js, .hermes/plans/2026-09-14_001100-graph-join-leak.md.
**Verify:** leak card gone, All back to 13, graph clean, drawer cycle passes; `npm test` green.

### Task 4: Hide split scope from changelog ✓ done

**Objective:** Apply the same hide marker to the over-limit splits plan; it was refactor hygiene, not a shippable entry.
**Files:** .hermes/plans/2026-09-13_235413-over-limit-splits.md.
**Verify:** splits card gone, no other card lost; `npm test` green.

*Shipped in 0c0f36a · Tasks 1–4 · phase-1.*
