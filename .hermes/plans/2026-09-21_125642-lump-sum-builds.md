# Lump-sum builds — week of Sep 21, 2026

> **For Hermes:** the weekly catch-all — one file per calendar week (Mon–Sun). A shipment no scoped plan covers appends `### Task N: …` under the phase it belongs to (or opens a new phase) and closes with `*Shipped in <sha> · Tasks a–b · phase-N.*`. At rollover the closing week is sealed and the next week opens its own file — a lump-sum plan never carries into a new week.

**Goal:** Nothing ships untraceable — every ad-hoc shipment lands in the week it shipped, with its sha recorded next to what changed.

**Architecture:** One file per week, phases numbered from 1 inside it; each phase clubs the week's related shipments and each task records what changed plus the sha that proves it, so `git log`, `.hermes/plan-links.json` and `/ds/#/changelog` agree. Week runs Mon 00:00 → Sun 23:59 local; a shipment opened before the seal finishes in its opening week's file.

**Tech Stack:** Vanilla JS + Vite, portfolio surface only (`main.js`, `src/views`, `src/styles`, `index.html`). Work inside `src/ds` gets a scoped plan of its own.

**Tags:** Layout, Motion, Component, Tooling

---

## Phase 1 — Hover inertia + deploy-on-approval SOP {#phase-1}

*Tags: Motion, Tooling*

*The week opened with two shipments outside any scoped plan: the list-popover Y-inertia build and the SOP change that makes deploys manual (no auto-push).*

| # | Task | Done when |
|---|------|-----------|
| 1 | List popover Y-inertia | follower regains a small Y spring for viewport flips, swaps rAF-batched; `lint:manage` holds |
| 2 | SOP: manual deploy | `AGENTS.md` / `.cursorrules` §5 — local commits auto, push only on the user's `y` |

### Task 1: List popover Y-inertia ✓ done

*2026-09-21 · feat(hover): tiny Y-inertia for list popover — viewport flip glides, rAF-batched*

**Objective:** New `src/views/list-follower.js` restores a small Y spring (viewport flips glide instead of snapping) while x stays immediate; `strip-viewer-helpers.js` drops the old direct-position path and batches swaps per rAF.

**Files:** `src/views/list-follower.js`, `src/views/strip-viewer-helpers.js`

**Verify:** Build green; fast scrub — popover glides on flips, no lag regression on straight travel.

### Task 2: SOP — manual deploy, push only on your `y` ✓ done

*2026-09-21 · chore(sop): manual deploy — push only on your y (no auto-push)*

**Objective:** Rewrite §5 of `AGENTS.md` + `.cursorrules`: commits stay local and attributable, `git push` / `vercel deploy` never run unauthorised (Vercel storage guard); the agent prepares + verifies locally and reports what would deploy. Push only on the explicit `y` / `push` / `deploy` / `ship it`.

**Files:** `AGENTS.md`, `.cursorrules` (workspace wrapper copies synced)

**Verify:** Wrapper `Portfolio Redo/AGENTS.md` + `.cursorrules` byte-identical to the repo copies.

*Shipped in 3f4cd90, 4961001 · Tasks 1–2 · phase-1.*

---

## Phase 2 — List rows, cinematic promo player, transit repair {#phase-2}

*Tags: Layout, Motion, Component*

*Three shipments landed outside any scoped plan: the list rows' brand-colour recall swatches, the promo player's cinematic rebuild (split into icons/markup/mount), and the transit hero-box selector repair.*

| # | Task | Done when |
|---|------|-----------|
| 3 | List rows — brand recall swatches | rows read title + colour chips + meta/date in two lines; canvas refine overrides the fallback palettes |
| 4 | Cinematic promo player | poster frame, buffer bar, pill + scrim bar on hover; modules split ≤100 lines, tokens stay in `tokens-fx` |
| 5 | Transit hero-box repair | `.case .hero-box` hides the real hero during the ghost flight (old `.case-media.hero-box` matched nothing) |

### Task 3: List rows — brand recall swatches ✓ done

*2026-09-21 · feat(list): brand colour recall swatches on work rows — two-line rows, canvas refine*

**Objective:** `brand-tokens.js` grows a 14-slug fallback palette map plus `attachBrandTokens()` (canvas quantize refines each row's chips from its preview image, extracted to `brand-tokens-extract.js`); `row()` renders main/sub rows with `data-swatches`; `.work` becomes a grid with a hover tint and a `--work-swatch` size token.

**Files:** `src/views/masonry/cards.js`, `brand-tokens.js`, `brand-tokens-extract.js`, `list-view.js`, `src/styles/pages-sections.css`

**Verify:** build green; rows show four chips refined to hue-spread colours once the preview loads; mobile flips swatches to 12px.

### Task 4: Cinematic promo player ✓ done

*2026-09-21 · feat(promo): cinematic player — icons split, poster frame, buffer bar, hover scrim*

**Objective:** `promo-video.js` becomes a barrel over `promo-video-markup.js` / `promo-video-mount.js` / `promo-video-icons.js`; the player gains a blurred pill, scrim bar, buffered range, keyboard shortcuts and a canvas-derived poster (with the atrium `.jpg` beside the mp4). The pill shows only while paused, the bar only while playing + hover.

**Files:** `src/views/promo-video*.js`, `src/styles/promo-video.css`, `src/styles/tokens-fx.css`, `src/views/project.js`, `public/videos/woman_standing_in_concrete_atrium_20260920203104.jpg`

**Verify:** build + `lint:tokens` green; play → bar fades in on hover, pause → pill only, buffer bar tracks progress.

### Task 5: Transit hero-box repair ✓ done

*2026-09-21 · fix(transit): hero-box selector — ghost hides the real hero again*

**Objective:** The list→detail flight hides the real hero while the ghost flies, but the selector had drifted to `.case-media.hero-box` — an element with both classes never existed, so the hero showed through. Now `.case .hero-box` across `main.js`, `transit.js` and `transit.css`.

**Files:** `src/main.js`, `src/views/transit.js`, `src/styles/transit.css`

**Verify:** list → click a row: hero stays hidden until the ghost lands; `data-reveal="replay"` still fires.

*Shipped in 265b15a, 8213488, 7514a72 · Tasks 3–5 · phase-2.*

---

## Phase 3 — Desktop popovers single-frame {#phase-3}

*Tags: Layout, Motion*

*Desktop had two concurrent preview frames per hover surface — grid showed cursor + portal, list showed peek + floating popover — while the spec wants a single frame per view.*

| # | Task | Done when |
|---|------|-----------|
| 6 | Single-frame popovers | grid = portal only (no cursor/tether), list = peek only (no floating popover), strip = one viewer; only one bordered preview visible per hover |
| 7 | Dark-mode single border | viewer has one 2px border whose color follows theme (light = ink dark, dark = ink light) and no inner padding gap |
| 8 | Restore connectors + list popover, single theme border | grid = cursor + tether + portal, list = peek + floating popover, strip = one viewer; all viewers single 2px border (dark `#15191f` in light, light `#f2f4f7` in dark) with no inner padding, no double frame |
| 9 | Light-in-light / dark-in-dark + connectors + zoom | viewer + cursor 2px + tether all `var(--color-surface)` (white `#fff` in light, dark `#1e2329` in dark), no inner padding, zoom re-measures via visualViewport |

### Task 6: Single-frame popovers ✓ done

*2026-09-21 · fix(popover): single-frame desktop previews — grid portal only, list peek only*

**Objective:** Grid removes the 32px viewfinder + tether lines — `viewer-markup.js`/`viewer-visibility.js`/`viewer-bind.js`/`viewer.js` keep only the 180px portal; `viewer.css` hides `.viewer-cursor`/`.viewer-lines`. List removes the floating `attachListViewer` popover — `list-view.js` keeps only `attachWorkPeek` (the sticky hero pane), so list hovers show a single right-column preview instead of peek + floating 180×240.

**Files:** `src/styles/viewer.css`, `src/views/masonry/viewer.js`, `viewer-markup.js`, `viewer-visibility.js`, `viewer-bind.js`, `src/views/masonry/list-view.js`

**Verify:** build green; `hover` grid → one portal only (no small box/lines); `hover` list row → one peek pane only (no floating viewer); `hover` strip icon → single `viewer--strip`.

### Task 7: Dark-mode single border ✓ done

*2026-09-21 · fix(popover): dark-mode single 2px border that tracks theme*

**Objective:** Viewer border was fixed white (`--stone-0`) with 4px inner padding — in dark the screenshot showed a double edge (black thickness + white). Now `--color-viewer-line` → `var(--color-ink)` so the 2px `--viewer-border` follows theme (dark ink in light, light ink in dark) and `--tether` too; `viewer.css` padding 0 removes the inner gap so the popover reads as one frame whose thickness is the black-only 2px but whose color matches the active theme.

**Files:** `src/styles/tokens-fx.css`, `src/styles/viewer.css`

**Verify:** build green; dark: border `rgb(242,244,247)` 2px on `data-theme="dark"`; light: `rgb(21,25,31)` 2px on `light`; screenshot single frame, no double edge.

### Task 8: Restore connectors + list popover, single theme border ✓ done

*2026-09-21 · fix(popover): restore grid connectors + list floating, single 2px theme border*

**Objective:** Task 6–7 hid the 32px cursor + SVG tether and removed `attachListViewer`, and the ink border showed dark in both modes. Restored `viewer-markup/visibility/bind.js` + `viewer.js` to keep cursor, lines and portal together, and `list-view.js` to keep `attachListViewer` + `attachWorkPeek` + `attachBrandTokens` so list has both the sticky peek pane and the floating `viewer--strip`. Kept the single-border fix: `viewer.css` `padding:0` (no inner 4px gap) + `--color-viewer-line:var(--color-ink)` in `tokens-fx` + explicit `#f2f4f7` overrides in `tokens-dark` for both `@media` and `[data-theme="dark"]`, so the single 2px border tracks theme (light `rgb(21,25,31)` dark on `light`, light `rgb(242,244,247)` on `dark`) and never shows dark in both.

**Files:** `src/views/masonry/viewer.js`, `viewer-markup.js`, `viewer-visibility.js`, `viewer-bind.js`, `src/views/masonry/list-view.js`, `src/styles/tokens-fx.css`, `src/styles/tokens-dark.css`, `src/styles/viewer.css`

**Verify:** build + ds-track green; `hover` grid → cursor + tether + portal (all 2px `rgb(21,25,31)` light / `rgb(242,244,247)` dark); `hover` list row → `#workPeek` + `viewer--strip` both visible, single borders; `hover` strip icon → single `viewer--strip`.

### Task 9: Light-in-light / dark-in-dark + connectors + zoom ✓ done

*2026-09-21 · fix(popover): light border in light, dark border in dark, connectors matching, zoom-aware*

**Objective:** Per latest ask the single 2px border must be light in light and dark in dark (not ink-contrast), and connectors must follow the same. Switched `--color-viewer-line/tether` to `var(--color-surface)` (`#fff` in light, `#1e2329` in dark via `tokens-dark` overrides for both media + `[data-theme]`), restored `viewer.css` cursor/lines/tether definitions (were `display:none`) with `padding:0`, and made `viewer-bind.js` listen to `visualViewport` resize/scroll in addition to window so zoom (pinch/browsertime) re-measures `rect`/`vx`. All viewers (grid portal, grid cursor, SVG lines, list floating `viewer--strip`, strip bar) now share the single 2px surface frame.

**Files:** `src/styles/tokens-fx.css`, `src/styles/tokens-dark.css`, `src/styles/viewer.css`, `src/views/masonry/viewer-bind.js`

**Verify:** build green; light: `border rgb(255,255,255) 2px`, `stroke color(srgb 1 1 1 /0.68)`; dark: `border rgb(30,35,41) 2px`, `stroke color(srgb 0.117 0.137 0.16 /0.68)`; `hover` grid shows cursor + tether + portal with matching colors; zoom in/out repositions via `visualViewport`.

*Shipped in 1bff4db, 46a5a21, 6e5d1f0, a4c6aad · Tasks 6–9 · phase-3.*

---

## Phase 4 — Next preview + mobile list polish + cursor hint {#phase-4}

*Tags: Component, Layout, Motion*

*Detail's "See what's next" becomes a two-column image+copy block fed from Framer, the work list gains a mobile thumb + bottom-rule polish and a cursor-hint, while CTA/contact styles extract to their own sheet.*

| # | Task | Done when |
|---|------|-----------|
| 10 | Next preview block | `next-preview.js`+`next-copy.js`+`next.css` render image+copy, `site.js` carries `nextImage`+dims, `public/images/next` optimized, `content/projects/*.json` and `public/admin/config.yml` carry the field |
| 11 | Mobile list + cursor hint | `cards.js` shows thumb + body on ≤640, `list-mobile.css` owns the 640 rules, `work-cursor.js/css` shows "Open ↗" at cursor, `strip-viewer-helpers.js` offsets viewer, `list-view.js` wires it |
| 12 | Detail/CTA responsive split | `case.css` reorders `.case-copy` on mobile via `display:contents`+order, `case-prose.css` drops old `.next`, `pages-cta.css` owns `.cta-band`/`.contact-hero`, links in `index.html`/`ds/index.html` updated, graph regenerated |

### Task 10: Next preview block ✓ done

*2026-09-21 · feat(next): Framer "See whats next" image+copy block*

**Objective:** Detail page no longer builds the next link inline — `next-preview.js` (`pictureMarkup` + `nextCopy`) renders `<a.next>` with `<div.next-copy>` (small+title+paras) + `<div.next-media>`, fed by the `nextImage` tuple (Frigid next preview, not hero) with its own `content/next-dims.json` sizes; `variants.json` and `public/images/next/*` provide responsive sets via `scripts/optimize-images.mjs`.

**Files:** `src/views/next-preview.js`, `src/data/next-copy.js`, `src/styles/next.css`, `src/data/site.js`, `content/next-dims.json`, `src/data/variants.json`, `public/images/next/*`, `public/admin/config.yml`, `src/views/project.js`, `src/styles/case-prose.css`, `index.html`, `ds/index.html`, `docs/graph.mmd`, `src/ds/arch-schema.json`

**Verify:** build + lint:tokens green; `/projects/tas-51` shows Next block with title + 2paras + `1000×1500` image, responsive 1→column at ≤800, `/admin` Next Preview Image uploads to `public/images/next`.

### Task 11: Mobile list + cursor hint ✓ done

*2026-09-21 · feat(list): mobile thumb + cursor hint + hairline frame*

**Objective:** Work rows gain a mobile presentation — `cards.js` emits `<span.work-thumb><img>` + `<span.work-body>` (title/meta/date split into `work-row--bottom`), `pages-sections.css` keeps desktop grid + `::after` hairline → frame on hover/focus, `list-mobile.css` owns the ≤640 overrides (flex rows, 96px thumb, clamped title, hidden peek). List hover gains `work-cursor.js/css` ("Open ↗" fixed at cursor, `work-cursor-active` hides native cursor) and `strip-viewer-helpers.js` offsets the viewer below the cursor hint.

**Files:** `src/views/masonry/cards.js`, `src/views/masonry/work-cursor.js`, `src/styles/work-cursor.css`, `src/styles/list-mobile.css`, `src/styles/pages-sections.css`, `src/views/masonry/list-view.js`, `src/views/strip-viewer-helpers.js`

**Verify:** desktop hover → hairline → frame, swatch lift, cursor hint "Open ↗" follows; mobile (≤640) → thumb 96px left, body column, no peek, `cursor:none` on desktop hover.

### Task 12: Detail/CTA responsive split ✓ done

*2026-09-21 · style(case): mobile re-order + CTA sheet split*

**Objective:** Extract landing CTA and contact hero styles out of the overgrown `pages-sections.css`/`case-prose.css` into `pages-cta.css` (owns `.cta-band` + `.contact-hero`, ≤640 padding), empty the old `.next` block from `case-prose.css`, and make `.case-grid` reorder on ≤800 via `display:flex` → `case-copy{display:contents}` + `order` (-2 title, -1 kicker, 0 media, 1 prose, 2 spec), keeping 40/60 desktop.

**Files:** `src/styles/case.css`, `src/styles/case-prose.css`, `src/styles/pages-cta.css`, `index.html`, `ds/index.html`, `docs/graph.mmd`, `src/ds/arch-schema.json`

**Verify:** `pages-sections.css` no longer contains `.cta-band`; desktop detail still 40/60 stacked promo; narrow → title → kicker → media → prose → spec order, no overflow.

*Shipped in 09114cd · Tasks 10–12 · phase-4.*

---

## Phase 5 — Hotfix — desktop order + subtle rims {#phase-5}

*Tags: Layout, Component*

*Phase 4 added a mobile thumb but moved desktop swatches/date to a third row and made the list-row bottom rim dark ink; restore two-row desktop and keep rims light-in-light.*

| # | Task | Done when |
|---|------|-----------|
| 13 | Desktop list order restored — mobile only | `cards.js` row() back to two rows `title+swatches / meta+date` inside thumb+body wrappers; desktop two rows, mobile thumb 96px + body column, swatches 12px, title clamp 2 |
| 14 | Subtle rims, no dark ink | `pages-sections.css` ::after stays hairline `var(--color-line)` — hover keeps panel tint + title underline, no frame-to-ink; `list-mobile.css` keeps thumb/peek mobile only, viewer/brackets stay `var(--color-surface)` light-in-light |

### Task 13: Desktop list order restored — mobile only ✓ done

*2026-09-21 · fix(list): desktop rows back to two, thumb mobile-only*

**Objective:** Mobile thumb was shipped with a three-row desktop (`title / meta / date+swatches`). Restore desktop to the two-row spec (`work-row--main` title+swatches, `work-row--sub` meta+date) while keeping `<span.work-thumb>` + `<span.work-body>` wrappers (`display:none` / `display:contents` on desktop, `flex` with 96px thumb on ≤640, body column, title clamp 2, swatches 12px). `list-mobile.css` now targets `.work-row`/`--main` not `--bottom`.

**Files:** `src/views/masonry/cards.js`, `src/styles/pages-sections.css`, `src/styles/list-mobile.css`

**Verify:** desktop list shows title+chips / meta·slug+date (two rows, swatches 14px), hover underlines title; mobile (≤640) shows 96px thumb left + body column, title 2-line clamp, no peek.

### Task 14: Subtle rims, no dark ink ✓ done

*2026-09-21 · fix(rims): list-row ::after stays hairline, popover stays surface*

**Objective:** Phase 4 made `.work:hover::after` grow to `var(--size-frame)` `var(--color-ink)` — a dark rim in light mode. Revert to hairline only (`var(--size-hairline)` `var(--color-line)` 10% ink light / 12% light in dark) and restore `work:hover .work-title` underline; viewer/cursor/brackets keep `var(--color-viewer-line)` = `var(--color-surface)` (white #fff light, #1e2329 dark, tether 68% mix) — light-in-light, dark-in-dark, zoom via `visualViewport`.

**Files:** `src/styles/pages-sections.css`, `src/styles/tokens-fx.css`, `src/styles/tokens-dark.css`, `src/styles/viewer.css`, `src/styles/card.css`

**Verify:** light: viewer border `rgb(255,255,255)` 2px, tether `color(srgb 1 1 1 /0.68)`, brackets same; dark: `rgb(30,35,41)`; list hover no dark frame, only panel tint + underline.

*Shipped in 2d9fbef, 9ea7373 · Tasks 13–14 · phase-5.*


---

## Phase 6 — List & case responsive polish + tooling {#phase-6}

*Tags: Layout, Component, Tooling*

*List rows get a true 3-column desktop grid and a 2×4 mobile grid with bottom-pinned date/swatches, hover tightens to a hairline frame, case detail reorders kicker/spec into the media column on mobile, and Playwright lands for visual checks.*

| # | Task | Done when |
|---|------|-----------|
| 15 | List rows — 3-col desktop + mobile grid | `cards.js` row() emits title + meta + bottom-row (date + swatches); `pages-sections.css` 641+ is 3-col, `list-mobile.css` ≤640 is 2×4 grid with date/swatches pinned bottom, peek hidden |
| 16 | Work hover + footer + about separation | `pages-sections.css` hover uses `::after` hairline frame (`--color-ink` on hover/focus), `footer.css` pad 32→16, `list-view.js` drops `aboutBlock` (list owns rows only) |
| 17 | Case detail — mobile kicker/spec in media | `project.js` splits kicker to `kicker--desktop`/`kicker--mobile` + duplicates `specHtml` in copy+media; `case.css` hides desktop kicker/spec on ≤800 and shows mobile kicker + spec grid in `.case-media` with orders |
| 18 | Tooling — Playwright + graph regen | `package.json`/`package-lock.json` add `playwright@1.63`, `docs/graph.mmd` + `src/ds/arch-schema.json` via `map-graph` |

### Task 15: List rows — 3-col desktop + mobile grid ✓ done

*2026-09-22 · feat(list): 3-col desktop grid + 2×4 mobile grid — date/swatches pinned*

**Objective:** `cards.js` row() now emits three rows — title-only main, meta (cat·slug), and bottom-row (date + swatches) — so desktop can be `1fr auto auto` and mobile `grid 2×4`; `pages-sections.css` adds `@media(min-width:641px)` 3-col placement (`date` col1 row2, `meta` col2, `swatches` col3) and keeps swatch lift; `list-mobile.css` becomes `display:grid` 2×4 with `work-thumb` 96px, `work-title` clamp 2, `work-date`/`work-swatches` pinned to row 4 bottom, `works` full-bleed, `peek` hidden.

**Files:** `src/views/masonry/cards.js`, `src/styles/pages-sections.css`, `src/styles/list-mobile.css`

**Verify:** build + lint:tokens green; desktop list 3-col (title row span, date left / meta mid / chips right); ≤640 thumb 96px left, title 2-line clamp, date bottom-left chips bottom-right.

### Task 16: Work hover + footer + about separation ✓ done

*2026-09-22 · fix(list): hover hairline frame + footer 16 + about out of list*

**Objective:** `pages-sections.css` hover/focus moves from text underline to `::after` hairline→frame (`var(--size-frame)` `var(--color-ink)` on `:hover`/`:focus-visible`); `footer.css` `padding` 32→16 trims the CTA bleed; `list-view.js` drops `aboutBlock` import and call so `listMarkup` ends after `</div></div>` — about is a separate route, not a list suffix.

**Files:** `src/styles/pages-sections.css`, `src/styles/footer.css`, `src/views/masonry/list-view.js`

**Verify:** build green; hover/focus shows bottom hairline → frame, no text underline shift; footer 16px pad; `/list` (masonry list view) no longer renders about block below rows.

### Task 17: Case detail — mobile kicker/spec in media ✓ done

*2026-09-22 · feat(case): mobile kicker/spec reorder — spec lives in media column*

**Objective:** `project.js` builds `kicker` once, then `mKicker` (`kicker--mobile`) and `dKicker` (`kicker--desktop`) plus one `specHtml` string reused twice — copy gets `dKicker` + `specHtml`, media gets `mKicker` + `specHtml` + `hero-box` + promo; `case.css` adds `.kicker--mobile/.spec` hidden on desktop, reorders on ≤800 via `case-copy{display:contents}` and `.case-media{order:-1}` with `kicker 0 / hero-box 1 / spec 2(grid 1fr) / promo 3`, so mobile reads kicker → hero → spec → promo + prose.

**Files:** `src/styles/case.css`, `src/views/project.js`

**Verify:** build green; ≥801 detail 40/60 with kicker/spec in copy; ≤800 kicker above hero, spec grid under hero, hero 100% width, no duplicate spec on desktop.

### Task 18: Tooling — Playwright + graph regen ✓ done

*2026-09-22 · chore(tooling): Playwright 1.63 + graph regen*

**Objective:** Add `playwright@^1.63.0` to `devDependencies` (lockfile + package) for headless visual/e2e checks; regen `docs/graph.mmd` and `src/ds/arch-schema.json` via `node scripts/map-graph.mjs` — list-view no longer imports `views_shared`, case tokens include `--leading-body` etc.

**Files:** `package.json`, `package-lock.json`, `docs/graph.mmd`, `src/ds/arch-schema.json`

**Verify:** `npm run ds:track` + `lint:tokens` + `build` green; `npx playwright --version` 1.63; graph `bbda71e` with 0 orphans.

*Shipped in 0d40da7 · Tasks 15–18 · phase-6.*

## Phase 7 — Special Viewer comma fix {#phase-7}

*Tags: Design System*

*One stray comma in the Viewer lab's render collapsed its whole pane to `NaN` and spilled Graduation outside the tabs — every Special tab showed Viewer Graduation in a middle column.*

| # | Task | Done when |
|---|------|-----------|
| 19 | Viewer render comma — pane restored | `viewer.js` L49 trailing comma removed; render is NaN-free/div-balanced, Graduation lives inside the Viewer pane, `plan:names` + `ds:track` + `test` green |

### Task 19: Viewer render comma — pane restored ✓ done

*2026-09-23 · fix(ds): viewer stray comma — Graduation back inside its pane*

**Objective:** Remove the expression-level comma after the figure-open template literal in `render()` — it turned the comma operator loose, discarded hero/Params/Specimen, unary-plussed the next literal to `NaN`, and the extra `</div>` closed the pane early so Graduation rendered as a permanent middle column on all 7 tabs.

**Files:** `src/ds/functions/viewer.js`

**Verify:** node render check (len 2657, no NaN, div net 0); headless Chromium — Glimmer active shows no stray pane child, Viewer active shows Params/Specimen/Graduation inside its pane; `plan:names`, `ds:track`, `test` green.

*Shipped in 5cc8405 · Tasks 19 · phase-7.*
