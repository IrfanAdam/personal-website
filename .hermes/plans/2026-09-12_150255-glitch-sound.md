# Glitch sound — single voice for today

> **For Hermes:** implement phase-by-phase, verify each with `npm run lint:tokens` + `npm run build`; then `npm run ds:track`, `npm test`, and visual check on `/ds/#/changelog`.

**Goal:** Procedural glitch audio that marks exactly one thing — today on the changelog (later: the single primary CTA per page). One recognizable tick, proximity-loud, sparse timing, hard off, honest after reload.

**Architecture:** New `src/views/glitch-sound.js` (≤100 lines, WebAudio only — no files): one shared `AudioContext` + one shared noise buffer; exactly one tracked element per page; fixed-identity voice (timing random, timbre ~fixed); squared proximity gain; single clock with manual-trigger cooldown. Wiring in `src/views/init-sound.js` (toggle + auto-scan), sounds only where `data-glitch-sound` is present.

**Tech Stack:** Vite 5 ESM, WebAudio (single AudioContext + noise buffer), vanilla JS/CSS, gates `lint-tokens.mjs` + `ds-track.mjs`

**Tags:** Function, Tooling

**Audit (grounded, 2026-09-12 — prior attempt, reverted):**
- 7 blobs × independent timers (1.5–4.8s) ≈ one burst every ~0.5s — constant chatter, not single glitches.
- 420px radius over blobs spaced ~1 blob-width apart: neighbours sound at ~0.8 while hovering today — proximity can't isolate; reads as "multiple sounds".
- Voice randomized bandpass 700–3900Hz + Q + decay + echo on every hit, plus a square-wave zip layer — no fixed identity; hiss one hit, whistle the next ("not clear").
- Hover tick + unlock-confirmation tick + scheduled tick could triple-fire on one gesture.
- Rule set from this audit: glitch (visual + audio) marks today only; other days stay silent/static.

---

## Phase 1 — single-voice engine [plan:2026-09-12_150255-glitch-sound.md#phase-1]
*Shipped in 4cdd253 · Tasks 1–2 · phase-1.*

### Task 1: engine with fixed identity ✓ done
**Files:** Create `src/views/glitch-sound.js` (≤100 lines).
**Do:** Shared context + noise buffer; one filtered-noise tick (~80ms, fast attack, band ~2kHz); randomize only timing + ±10% pitch/level; squared proximity gain (~260px radius); hover/tap = full tick with 600ms retrigger floor; manual trigger pushes next scheduled tick back (one clock); `prefers-reduced-motion` = silent; gesture unlock with first-click confirmation tick on the hovered element.
**Verify:** `lint:tokens` + `build` pass; live: zero `createOscillator` calls, single spaced ticks in buffer-source log.

### Task 2: wiring + hard off ✓ done
**Files:** Create `src/views/init-sound.js`; modify `src/main.js`, `src/ds/ds.js` (side-effect imports).
**Do:** `sound: on/off` toggle (site header + `.ds-controls`), persisted `adam-sound`; off suspends the context (physical silence, not a flag); MutationObserver auto-scan attaches new `[data-glitch-sound]` nodes only, never resets live timers.
**Verify:** Toggle off → buffer-source counter frozen across blob clicks; on → resumes; reload keeps choice, first click confirms audibly.

## Phase 2 — today-only rollout [plan:2026-09-12_150255-glitch-sound.md#phase-2]
*Shipped in 3cd65b4 · Tasks 3–4 · phase-2.*

### Task 3: changelog today blob ✓ done
**Files:** Modify `src/ds/pages-changelog.js`.
**Do:** `data-glitch-sound="5000-9000"` on the today blob only; all other days silent/static (keep filter + border hover).
**Verify:** Exactly 1 `[data-glitch-sound]` on `/ds/#/changelog`; hover today = full tick; neighbours silent.

### Task 4: pipeline + lab note ✓ done
**Files:** None (process).
**Do:** Commit per phase with `[plan:2026-09-12_150255-glitch-sound.md#phase-N]`; `npm run ds:track`; `npm test`; Miller-columns check on `/ds/#/changelog`.
**Verify:** Manifest lists the plan commits; DS lab (`#/functions/glitch`) notes the today-only rule (already in lab copy).

## Phase 3 — audibility hotfix [plan:2026-09-12_150255-glitch-sound.md#phase-3]
*Shipped in 020453b · Tasks 5–6 · phase-3.*

### Task 5: scheduled audibility + unlock reliability ✓ done
**Files:** Modify `src/views/glitch-sound.js`, `src/views/init-sound.js`.
**Do:** Scheduled tick gains a base level (0.35 + 0.65·prox²) so it stays audible even when pointer is far from timeline; up decay to ~100ms and peak to 0.8 for clearer tick; listen for `pointerdown`/`touchstart`/`keydown` (not just `click`) to resume the context on any gesture; `play()` no longer creates a silent BufferSource when context is `suspended` — it awaits `resume()` then plays, and suppresses only duplicate rapid triggers via the 600ms floor (single clock preserved); keep zero `createOscillator`.
**Verify:** Reload → `localStorage adam-sound` defaults on, `matchMedia reduce` false → scheduled tick fires every 5–9s with `createBufferSource` count rising even when pointer is far; devtools: `AudioContext.state === 'running'` after first pointerdown anywhere.

### Task 6: hover target + lab test + pipeline ✓ done
**Files:** Modify `src/ds/changelog.css` (enlarge today hit-area), `src/views/glitch-sound.js` (accept hover within 8px proxy), `src/ds/functions/glitch.js` (add “Test sound” button that calls `glitch-sound` directly), `src/ds/pages-changelog.js` keeps single `data-glitch-sound`.
**Do:** Today blob gets `padding` proxy via `::before` 12px so 16→40px hit; JS also treats `pointermove` within 18px as hover (full tick, respects 600ms floor); lab button lets user hear the tick without hunting the blob; pipeline: `lint:tokens` + `build` pass, `ds:track` shows Phase 3, `npm test` green.
**Verify:** On `/ds/#/changelog` hover today (even near-miss) → full tick, neighbours still silent; lab `Test sound` → single tick; toggle off freezes `BufferSource` count; on resumes; reload → first pointerdown anywhere confirms audibly if over today.

## Phase 4 — palette + timing sync + changelog stability [plan:2026-09-12_150255-glitch-sound.md#phase-4]
*Shipped in e422e76 · Tasks 7–9 · phase-4.*

### Task 7: cyberpunk voice palette + wiring ✓ done
**Files:** Create `src/views/sound-palette.js`; Modify `src/views/glitch-sound.js`, `src/views/init-sound.js`, `src/styles/base.css`, `src/ds/specimens.css`.
**Do:** `sound-palette.js` — 6 WebAudio voices (tick/static/blip/chime/hum/data, no samples); `glitch-sound.js` → multi-voice `pickKind` + `hum` default, `parseAttr` for `data-glitch-sound="5000-9000 hum"` pools; `init-sound.js` → icon-only `snd-pill` (waves/.on vs slashed/off) with aria-safe `syncUI`; `base.css` → `.snd-pill` + `.pill.on` outline; `specimens.css` → `.fx-split/.fx-panel` for lab layout.
**Verify:** `lint:tokens` pass (≤100 lines css), lab chip `hum` etc. audible via `playSound(type)`, toggle shows speaker waves/slash.

### Task 8: lab overhaul with sync on glitch ✓ done
**Files:** Modify `src/ds/functions/glitch.js`.
**Do:** Lab now `grid-card` + sound panel (voice/pitch/length/level, proximity toggle, chips); `playViaLabProx` with `labProx()` 260px linear + `paletteSynth` direct (proximity) vs `playSound` (static); `sample on glitch: on` listens `animationstart/iteration` and fires after `0.88*dur` via `pending` map so audio aligns to `fx-glitch` burst (2120ms @2400ms); `pointermove` tracks lab proximity; cleanup clears pending + suspends labCtx.
**Verify:** `/ds/#/functions/glitch` → play hum 320ms proximity-loud, toggle off → level slider, sync on → tick at visual burst, not at 0%; `build` green.

### Task 9: timing sync + changelog stability ✓ done
**Files:** Modify `src/views/glitch-sound.js`, `src/ds/changelog.css`, `src/ds/pages-changelog.js`.
**Do:** `glitch-sound.js` → `AT=0.88` phase-sync: `untilGlitch()` via `el.getAnimations()` (glitch @89%) → `schedule()` waits `lo+rand*(hi-lo)` then `resume()` early + `phase` delay to burst; `durMs()` from `--dur-glitch`; `g` base `0.34+0.56*p` so far stays audible; `isConnected` guards + `detach` clears phase. `changelog.css` → `.ds-day.is-today` (not `button` only) animates so empty today also jitters, `is-today::after` + `is-today` share reduce-motion. `pages-changelog.js` → `paint()` caches `graph()` once (`dataset.ready`) then only toggles `.on`/`aria-pressed` on `[data-day]` — graph no longer rebuilds on every filter/drawer open, animation phase stays steady.
**Verify:** `/ds/#/changelog` today cell jitters every 2.4s, scheduled `hum` fires locked to burst (1–2% before displacement) even when pointer far; filtering tags/days doesn't reset animation; `npm run ds:track && npm test` green, single `[data-glitch-sound]` still.

## Phase 5 — kind-first sources + file defaults + element engine [plan:2026-09-12_150255-glitch-sound.md#phase-5]
*Shipped in 05d70ef · Tasks 10–13 · phase-5.*

### Task 10: vendored web-sounds + per-slot source store ✓ done
**Files:** Create `public/sounds/load.wav`, `public/sounds/loading.mp3`, `public/sounds/scifi-weapon.wav`, `src/views/sound-files.js`, `src/views/sound-source.js`.
**Do:** Vendored `IrfanAdam/web-sounds` locally (`/sounds/*` + raw.githubusercontent fallback, one cached buffer per id); `sound-source.js` per-slot `{ kind, voice, file }` for `load/glitch/scramble` persisted as JSON in `localStorage 'adam-sound-src'`; load default is file `loading.mp3`.
**Verify:** `dist/sounds/` serves 200s (`load.wav` 20688b/117ms stereo, `loading.mp3` 16128b/0.5s, `scifi-weapon.wav` 886ms); fresh lab shows `load: file loading.mp3`.

### Task 11: kind-first pickers + file-aware controls ✓ done
**Files:** Modify `src/ds/functions/glitch.js`, `src/ds/functions/scramble.js`.
**Do:** Glitch lab `Source — kind first` panel (load+glitch kind→voice→file, file/voice previews, `reset saved picks`); scramble toolbar kind→voice→file row; file mode disables procedural-only controls (voice/pitch/length/chips) while level+proximity stay live as file gain; `saySrc`/`syncSrcCtl` keep panel honest; status drops `ctx:` (engine no longer live).
**Verify:** Flip kind both ways in both labs — selects disable appropriately, picks persist, no errors; `npm test` green.

### Task 12: title reveal default on page titles ✓ done
**Files:** Create `src/views/title-reveal.js`, `src/views/slot-sound.js`; Modify `src/views/masonry.js`, `src/views/project.js`, `src/views/contact.js`, `src/views/init-sound.js`.
**Do:** `title-reveal.js` 0.9s word-mode decoder (`tweenLength:false`, height lock→release, per-line stagger for multi-span heroes, preserves `ink`/`muted`); `slot-sound.js playSlot(slot,level)` unifies procedural/file with mute/gesture guards + 1200ms per-slot dedupe so load timer and reveal never double; `init-sound.js` load timer now `playSlot('load')`.
**Verify:** `/#/projects/helix` mid-reveal scrambles then lands exact, lock released; multi-line contact hero no stutter; `npm test` green.

### Task 13: element engine — audible where live WebAudio is silent ✓ done
**Files:** Create `src/views/element-sound.js`, `src/views/audio-ctx.js` (+`resetCtx`, exported `encodeWavBuffer`); Modify `src/ds/functions/glitch.js`, `src/ds/functions/scramble.js`, `src/views/slot-sound.js`, `src/views/init-sound.js`.
**Do:** Live `AudioContext` output proven dead in Safari (status `played:raw-880Hz`, no tab audio indicator) while `<audio>` (test B) audible — so all playback now routes through the element pipeline: files play directly, procedural voices render offline via the same synth to a WAV blob and play as elements; test A kept as live-path diagnostic, `reset ctx` button added.
**Verify:** Both labs mount and report honestly (status updates, no resume hangs); build green; ear-check on `:5173` Safari.

## Phase 6 — hover hum through the element engine [plan:2026-09-12_150255-glitch-sound.md#phase-6]
*Shipped in 9196057 · Tasks 14–16 · phase-6.*

### Task 14: proximity hum wiring ✓ done
**Files:** Modify `src/ds/changelog-events.js` (≤100 lines, sections + Exports map intact).
**Do:** Moving the pointer within 260px of the today cell fires one `playVoice('hum', …)` through the element pipeline (offline-rendered WAV → `<audio>`, the only audible path in Safari) — 1.5s approach floor, gain `0.55` inside the cell and `0.06–0.26` in the radius, scheduled with `untilGlitch(cell)` so the hum lands on the visual jitter burst instead of at 0%. Silent under `prefers-reduced-motion`, and the sound toggle still hard-mutes via `playVoice`'s own guard. Pending timer cleared on unbind.
**Verify:** Sound on → pointer near today plays one hum per approach (no machine-gun retrigger); sound off → zero `Audio` constructions; reduced motion → silent; no console errors.

Verified on a fresh build: sound on + pointer inside the today cell → exactly one `Audio` construction (element pipeline), 6 rapid moves over ~1.2s still produce one hum (approach floor holds), pointer 420px away stays silent, `adam-sound` off → 0, `prefers-reduced-motion: reduce` → 0, zero console errors.

### Task 15: retire the calendar attribute hum ✓ done
**Files:** Modify `src/ds/changelog/graph.js`.
**Do:** Drop `data-glitch-sound="5000-9000 hum"` from the today cell. That attribute routed the hum through the glitch engine's live `AudioContext`, which the element-engine phase proved silent in Safari — so the deployed calendar never hummed. The visual jitter (`.ds-day.is-today`) is untouched, and the attribute API stays documented for other surfaces in the lab.
**Verify:** No `[data-glitch-sound]` remains in the calendar markup; the today cell keeps `.is-today` + its animation; lab copy still documents the attr API.

Verified: the calendar renders `[data-glitch-sound]` count 0 while `.ds-day.is-today` keeps its class, `data-day`, `data-tip` and jitter animation; the lab copy still documents the attr API.

### Task 16: verify + ship ✓ done
**Files:** None (process) + `changelog-manifest.json` refresh.
**Do:** `lint:manage`, `lint:tokens`, `npm run build`, `smoke` on 5199; browser proof on a fresh load; commit with `[plan:2026-09-12_150255-glitch-sound.md#phase-6]`, `npm run ds:track`, track-refresh commit, push so the deployed build carries it.
**Verify:** Gates green; `/ds/#/changelog` hums locally through `<audio>`; deployed bundle contains the proximity wiring after the push.

Verified: `lint:manage` clean, `lint:tokens` 36 stylesheets + 33 JS, `npm run build` ✓, `smoke` on 5199 ✓; `ds:track` 0 unlinked; feature commit `9196057` + close-out + `chore(ds): track refresh`, then pushed so the Vercel build carries the hum.

---
