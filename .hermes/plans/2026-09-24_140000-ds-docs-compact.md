# DS build — 24 Sep single build: docs compact + Sound + primitives

**Goal:** Ship the day's DS work as one build — docs tabs flow into content (no 50px gap), Sound returns to Foundations as the eighth pane, and primitives are readable, honest and easy to scan like Library/Special. One set of tasks, one build — no split unless you explicitly ask.
**Architecture:** `src/ds/ds-tabs.css` owns tab→panel rhythm (hairline, flush `ds-sec`); `src/ds/pages-foundations.js` hosts Sound as eighth vertical pane; `src/ds/primitives/*` + `src/ds/component.js` + `src/ds/pages-primitives.js` compose 9 vertical sheets with `ds-spec block` previews, `tok`-label specs below tabs, and Library-like lede. Single plan file, hidden subsidiaries, `plan-links.json` remaps earlier shas.
**Tech Stack:** Vanilla JS + Vite, `tabs({vertical:true})`, `ds-tabs.css`, `foundations/sound/*`, `primitives/*`, `component.js`, token CSS, `ds-track`, `plan-links` remapping.
**Tags:** Design System, Component

---

## Phase 1 — Glitch tabs compact flow {#phase-1}
*Tags: Design System, Component*
*Custom horizontal tabs above the Glitch lab currently leave ~50px of dead space (tablist `margin-bottom` + `.ds-sec` `margin-top` + `.ds-spec` `margin`). Match vertical-tabs rhythm: one hairline, content 10–12px below it.*

| # | Task | Done when |
|---|---|-----------|
| 1 | Tab→panel rhythm owned in `ds-tabs.css` | `.ds-tablist--line + [data-tab-panel] > .ds-sec` flush (no top border, small `margin-top`), same for code panel; `plan:names` + `ds:track` + `npm test` green, `/ds/#/special` Glitch screenshot shows single line, content ≤16px below it |
| 2 | Verify no double rule / no clipping | `lint:tokens` clean, lab `fx-panel`/`fx-split` still wraps, no horizontal overflow |
| 3 | Drop redundant `Code` heading | `src/ds/functions/glitch.js` code panel has no `<h2>Code</h2>` — tab label already says Code; `ds-code` sits flush 10px below hairline |
| 4 | Remove Special footer DO note | `src/ds/pages-special.js` no longer appends `DO — Tune in the lab…` footer; `/ds/#/special` ends at pagination without extra note |

### Task 1: Tab→panel rhythm owned in `ds-tabs.css` ✓ done
**Objective:** Glitch Lab/Code tabs flow directly into the next section — one hairline, compact.
**Files:** `src/ds/ds-tabs.css`
**Verify:** `npm run plan:names` 0 missing, `npm run ds:track` tagged==phased, `npm test` green, screenshot `/ds/#/special` (Glitch) shows no loose gap.

### Task 2: Verify no double rule / no clipping ✓ done
**Objective:** No second hairline floating in a gap; sound panel still wraps.
**Files:** `src/ds/ds-tabs.css`, `src/ds/ds-lab.css` (read only)
**Verify:** `plan:names` + `ds:track` + `build` green; manual check Glitch lab at 1200 and 640.

### Task 3: Drop redundant `Code` heading ✓ done
**Objective:** Code tab no longer repeats `<h2>Code</h2>` — tab is the label.
**Files:** `src/ds/functions/glitch.js`
**Verify:** `/ds/#/special` Glitch Code tab shows `ds-code` flush under hairline, no heading; `build` green.

### Task 4: Remove Special footer DO note ✓ done
**Objective:** `/ds/#/special` no longer shows the global `DO — Tune in…` footer; pagination follows directly.
**Files:** `src/ds/pages-special.js`
**Verify:** `/ds/#/special` at any vertical tab shows no DO block above `← Prev Library`/`Next →`; `build` green.

*Shipped in a56cb9d · Tasks 1–4 · phase-1.*

---

## Phase 2 — Special labs tabified — Lab | Code, Graduation removed {#phase-2}
*Tags: Design System, Component*
*Every Special lab (GridReveal, Shimmer, Rise, Viewer, Glimmer) currently stacks Lab + Tokens + Single source + Graduation as separate `ds-sec` blocks — too many, and Graduation is noise. Like Glitch/Scramble, each keeps its hero intro and splits the rest into exactly two horizontal tabs: Lab (interactive demo) | Code (code snippet + tokens table, Graduation merged/deleted).*

| # | Task | Done when |
|---|---|-----------|
| 5 | Shimmer — Lab | Code | `src/ds/functions/shimmer.js` has `data-shimmer-tabs` Lab/Code; Lab panel = `fx-shimmer` demo + controls, Code panel = CSS code + Tokens table; mount binds tabs; `build` green, `/ds/#/special` Shimmer tabs switch, Code shows no extra `ds-sec` gap |
| 6 | Rise — Lab | Code | `src/ds/functions/rise.js` same; Lab = `fx-rise` demo, Code = Tokens table (graduation note removed); mount binds tabs |
| 7 | Viewer — Lab | Code, Graduation removed | `src/ds/functions/viewer.js` Lab = Specimen hover card, Code = Params table + code + Graduation table merged (header "Tokens", note removed); no standalone Graduation `ds-sec` |
| 8 | Glimmer orb — split + Lab | Code | `src/ds/functions/glimmer-orb.js` thin composer + `glimmer-lab.js` + `glimmer-code.js` (or inline split) to stay ≤100 lines; Lab = canvas + controls, Code = `attachGlimmerOrb` snippet + Single source table; mount binds tabs + defers canvas until visible |
| 9 | GridReveal — Lab | Code | `src/ds/functions/grid-reveal.js` + `grid-reveal-lab.js` + `grid-reveal-docs.js` wired into tabs; Lab = `labSection` demo, Code = `docsSections` (Single source + Usage); mount binds tabs |

### Task 5: Shimmer — Lab | Code ✓ done
**Objective:** Shimmer no longer stacks Lab + Tokens; horizontal tabs, Code holds Tokens.
**Files:** `src/ds/functions/shimmer.js`
**Verify:** Tabs switch, `lint:tokens` + `build` green, 640 no overflow.

### Task 6: Rise — Lab | Code ✓ done
**Objective:** Rise Lab/Code tabs, graduation phrasing removed.
**Files:** `src/ds/functions/rise.js`
**Verify:** Same.

### Task 7: Viewer — Lab | Code, Graduation removed ✓ done
**Objective:** Viewer Specimen is Lab, Params+Graduation is Code (single Tokens table), no extra Graduation page.
**Files:** `src/ds/functions/viewer.js`
**Verify:** Lab tab shows hover card, Code shows Params table + code, no Graduation heading.

### Task 8: Glimmer orb — split + Lab | Code ✓ done
**Objective:** Glimmer orb stays ≤100 lines via split; tabs as above.
**Files:** `src/ds/functions/glimmer-orb.js`, `src/ds/functions/glimmer/**` (if split)
**Verify:** Canvas still defers start until tab visible; `lint:tokens` green.

### Task 9: GridReveal — Lab | Code ✓ done
**Objective:** GridReveal hero + tabs; Lab = 280px stage, Code = Single source + Usage.
**Files:** `src/ds/functions/grid-reveal.js` (and lab/docs helpers)
**Verify:** Lab controls work, Code shows tables, no standalone stacked secs.

*Shipped in 6d5880e · Tasks 5–9 · phase-2.*

---

## Phase 3 — Sound returns to Foundations as the eighth pane {#phase-3}
*Tags: Design System*
*Elements Sound becomes the eighth Foundations subtab; the Foundations copy says eight definitions, the sidebar no longer lists Sound separately, and old hashes still land. Folded from 2026-09-24_150000-sound-to-foundations.md Phase 1.*

| # | Task | Done when |
|---|---|-----------|
| 1 | Foundations adds Sound pane | `pages-foundations.js` imports `pane-sound` as eighth pane (after FX, before Tokens), lede says Eight definitions, `outerInitial` maps `sound→7`, tabs count 8 |
| 2 | Routes + redirect | `routes.js` removes `#/sound` Elements entry; `ds.js` maps `#/sound` and `#/functions/sound` → `#/foundations` (Sound pane), sidebar no longer shows Sound under Elements |
| 3 | Mount wiring + verify | `pages-foundations.js` mount calls `mountSoundBoard(root)` and cleans up; `plan:names`, `ds:track`, `test` green; `/ds/#/foundations` shows 8 vertical tabs with Sound active and playable |

### Task 1: Foundations adds Sound pane ✓ done
**Objective:** Sound is the eighth definition alongside Color·Type·Space·Shape·Motion·FX·Tokens.
**Files:** `src/ds/pages-foundations.js`, `src/ds/foundations/pane-sound.js`
**Verify:** `/ds/#/foundations` vertical tabs = 8, Sound pane renders intro + Lab/File/Code line tabs + scope.

### Task 2: Routes + redirect ✓ done
**Objective:** No Elements Sound entry; legacy hashes land on Foundations.
**Files:** `src/ds/routes.js`, `src/ds/ds.js`
**Verify:** Sidebar Elements = Primitives·Library·Special·Patterns (no Sound); `#/sound` redirects to `#/foundations`.

### Task 3: Mount wiring + verify ✓ done
**Objective:** Sound board is playable inside Foundations.
**Files:** `src/ds/pages-foundations.js`, `src/ds/foundations/sound/board.js`
**Verify:** Playing a voice/file on Foundations Sound tab repaints scope; gates green.

*Shipped in c7dd23c · Tasks 1–3 · phase-3.*

---

## Phase 4 — Primitives: 9 sheets readable, token-audited {#phase-4}
*Tags: Design System, Component*
*Packed `.join('')` + split `s\n    .kind` hacks pass the 100-line gate by collapsing, not by extraction — violates one-statement-per-line and readability. Folded from 2026-09-24_160000-primitives-complete.md Phase 1.*

| # | Task | Done when |
|---|---|-----------|
| 1 | Button sheet readable | `src/ds/primitives/btn.js` ≤100 lines, one statement per line, no nested ternary, header + export map + section banners, `var(--token)` list audited (exists), `btnSheet()` renders primary/secondary/ghost + sm/md + disabled + dark probe, `build` green |
| 2 | Tag + Kicker sheets readable | `tag.js` + `kicker.js` same: ≤100, readable, tokens audited, anatomy/behaviour plain, `build` green, lint clean |
| 3 | Divider + Badge sheets readable | `divider.js` + `badge.js` same: readable, tokens audited, no raw px, `build` green |
| 4 | Field sheets readable (field + helpers) | `field.js` + `field-meta.js` + `field-code.js` each ≤100, no packing, tokens audited (`--color-input-*`, `--size-frame` etc), control/state/disabled/hint/error + dark probe, `build` green |
| 5 | Toggle + Avatar + Logo sheets readable | `toggle.js` + `avatar.js` + `logo.js` same: ≤100 readable, tokens audited, Toggle checkbox semantics, Avatar square doctrine + fallback, Logo wordmark authored case, `build` green |

### Task 1: Button sheet readable ✓ done
**Objective:** Button is the canonical anchor — primary/secondary/ghost × sm/md + disabled + dark.
**Files:** `src/ds/primitives/btn.js`
**Verify:** ≤100 lines, one-statement-per-line, `lint:tokens` var-only, `build` green.

### Task 2: Tag + Kicker sheets readable ✓ done
**Objective:** Chip + eyebrow — minimal static sheets but honest tokens/anatomy.
**Files:** `src/ds/primitives/tag.js`, `src/ds/primitives/kicker.js`
**Verify:** Same gate.

### Task 3: Divider + Badge sheets readable ✓ done
**Objective:** Rule + chip — hairline and accent vs muted AA.
**Files:** `src/ds/primitives/divider.js`, `src/ds/primitives/badge.js`
**Verify:** Same.

### Task 4: Field sheets readable ✓ done
**Objective:** Field is the largest primitive — controls split across three files for budget by extraction.
**Files:** `src/ds/primitives/field.js`, `src/ds/primitives/field-meta.js`, `src/ds/primitives/field-code.js`
**Verify:** Each ≤100, readable sections, tokens audited, `build` green.

### Task 5: Toggle + Avatar + Logo sheets readable ✓ done
**Objective:** Switch, square avatar, wordmark — close the 9.
**Files:** `src/ds/primitives/toggle.js`, `src/ds/primitives/avatar.js`, `src/ds/primitives/logo.js`
**Verify:** Same.

*Shipped in ae132a8 · Tasks 1–5 · phase-4.*

---

## Phase 5 — Primitives: composer + shared chrome, ≤100 lines {#phase-5}
*Tags: Design System, Component*
*Composer still carries a >120ch lede line, component.js line 27 over budget, playground/mount missing export map, and pages-primitives hero copy lags the sheet template. Folded from 2026-09-24_160000-primitives-complete.md Phase 2.*

| # | Task | Done when |
|---|---|-----------|
| 6 | `pages-primitives` composure | `src/ds/pages-primitives.js` ≤100, one-statement-per-line, header + export map + banners, hero `<h1>` + lede ≤120ch, 9-pane vertical tabs, `mount` delegates to `mountComponent`, `build` green |
| 7 | `component.js` + `playground` chrome | `src/ds/component.js` ≤100 readable (extract `tokHTML`/`tabsHTML` helpers, no >120ch), `src/ds/playground/mount.js` gains export map + banners, `src/ds/playground.js` ≤100 + map + banners, `lint-manage` + `lint:tokens` green |

### Task 6: `pages-primitives` composure ✓ done
**Objective:** Elements → Primitives reads like Foundations — one story per tab, no overflow.
**Files:** `src/ds/pages-primitives.js`
**Verify:** ≤100, hero lede wrapped, 9 panes vertical, `build` green, no >120ch.

### Task 7: `component.js` + `playground` chrome ✓ done
**Objective:** Shared sheet chrome is scannable — tokens + copy + tabs own their lines.
**Files:** `src/ds/component.js`, `src/ds/playground.js`, `src/ds/playground/mount.js`
**Verify:** Each ≤100 or split, banners + export map, `lint-manage` + `lint:tokens` + `build` green.

*Shipped in 569aa24 · Tasks 6–7 · phase-5.*

---

## Phase 6 — Primitives: proof, graph, gates green {#phase-6}
*Tags: Design System, Component*
*Exercise is only complete when gates prove it and pixels match intent — lint-manage 0, lint:tokens var-only, build ok, primitives tabs render at 1280 and 640, graph shows primitives 9 sheets. Folded from 2026-09-24_160000-primitives-complete.md Phase 3.*

| # | Task | Done when |
|---|---|-----------|
| 8 | Lint + build + graph | `npm run lint:tokens` var-only clean, `node scripts/lint-manage.mjs` 0 (or only unrelated pre-existing), `npm run build` green, `node scripts/map-graph.mjs` + `docs/graph.mmd` + `arch-schema.json` fresh |
| 9 | Visual proof — 9 tabs | Screenshots `/ds/#/primitives` Button·Tag·Kicker·Divider·Badge·Field·Toggle·Avatar·Logo at 1280 and 640 show Preview/Code tabs, knobs below, tokens as `.tok` chips, no overflow |

### Task 8: Lint + build + graph ✓ done
**Objective:** Gates are the evidence — not a manual claim.
**Files:** `scripts/*`, `docs/graph.mmd`, `src/ds/arch-schema.json`
**Verify:** `lint:tokens` clean, `lint-manage` 0 new, `build` green, `graph` committed.

### Task 9: Visual proof — 9 tabs ✓ done
**Objective:** Pixels prove the exercise — every sheet looks like the same template.
**Files:** (screenshots)
**Verify:** 9 screenshots attached, knobs live, Code reflects knobs, dark probe where applicable.

*Shipped in c7d3c4d · Tasks 8–9 · phase-6.*

---

## Phase 7 — Primitives refinements — tabs, intro, sections like Library/Special {#phase-7}
*Tags: Design System, Component*
*Primitives tabs, intro and sections were denser than Library/Special — preview floated without a card, What/When headings pushed the demo below the fold, lede carried a slash and an extra Overview link. This phase makes primitives match Library/Special: intro two sentences like Library, preview wrapped in `ds-spec block`, specs as `tok` labels below tabs, demo immediate after sub.*

| # | Task | Done when |
|---|---|-----------|
| 10 | Intro like Library/Special | `pages-primitives.js` lede 2 sentences + knobs note, no ←Overview link, ≤120ch per line, hero matches Library tone |
| 11 | Preview container like Library | `component.js` preview wrapped in `ds-spec block` (border + pad), tabs `variant:line` Preview/Code same as `playground` |
| 12 | Specs below tabs, tok labels | `component.js` What/When as `<span class="tok">What</span>` / `When` below Preview/Code (not `h3` above), so demo immediate after sub like Library |

### Task 10: Intro like Library/Special ✓ done
**Objective:** Elements → Primitives lede reads like Library (2 sentences, knobs note) and Special, no extra link.
**Files:** `src/ds/pages-primitives.js`
**Verify:** `/ds/#/primitives` hero shows "Each sheet is one primitive — what it is, how it looks, and which tokens it uses. Preview is live; Code holds snippet + tokens. Knobs stay below …" no overflow.

### Task 11: Preview container like Library ✓ done
**Objective:** Primitives preview has the same bordered card as Library's `ds-spec block`.
**Files:** `src/ds/component.js`
**Verify:** Button/Tag etc preview inside `ds-spec block`, border hairline, padding, Code tab same.

### Task 12: Specs below tabs, tok labels ✓ done
**Objective:** What/When not pushing demo — specs sit below tabs, demo immediate after sub.
**Files:** `src/ds/component.js`
**Verify:** Title → sub → Preview/Code → What/When (`tok` labels) → knobs; matches Library flow.

*Shipped in <pending> · Tasks 10–12 · phase-7.*
