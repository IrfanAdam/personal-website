# ADAM/DS Maturity — Implementation Plan

> **For Hermes:** Use subagent-driven-development skill to implement this plan task-by-task.

**Goal:** Restructure ADAM/DS into a human-traceable system: narrative Overview, single-sheet Foundations (ramp → semantic → usage), uniform Components with tabs + playground, and split Functions with GridReveal as a shared function.

**Architecture:** Keep hash router + `playground()` + `specimens.js` infra; reshape IA, add one shared `tabs()` + `tokenTrace()` helper, split `pages-functions.js` into per-function modules. No new styles, no pixel changes to live site.

**Tech Stack:** Vanilla JS modules, `tokens.css` as single source, `npm run test` (lint:tokens + vite build) as gate.

---

## Current context / assumptions

- Routes today (`src/ds/ds.js:11-20`): Overview, Foundations, Tokens, Primitives, Library·Landing, Components, Patterns·Quality, Functions. Foundations↔Tokens and Primitives↔Components↔Library overlap.
- Token truth: `src/styles/tokens.css` (primitives → semantic → legacy aliases). Consumption rule: UI uses `var(--color-*/--text-*/--space-*)` only.
- Infra to reuse: `src/ds/playground.js` (knobs → preview → code), `src/ds/specimens.js` (swatch, ramp, code, note, probeTheme), `src/ds/fx-lab.js` (cell-grid lab imports `cells.js buildTree`).
- Constraints: every stylesheet ≤100 lines, no raw hex outside `tokens.css`, `npm run test` green per phase. No commit/push unless asked.

## Proposed IA (target)

- Start → Overview (narrative + moodboard + how-to-read)
- Foundations → Color | Type | Space/Layout | Shape | Motion (each: ramp/scale → semantic map → final usage; merges old Foundations + Tokens)
- Components → uniform template per component (Usage → Anatomy → Behaviour → Preview/Code/Tokens tabs → Playground)
- Functions → index + GridReveal (shared) | Shimmer | Rise | Viewer (each its own route; GridReveal notes Grid-item + Project-hero usage)

## Phase 0 — Baseline + IA skeleton (no visual change)

### Task 1: Snapshot baseline ✓ done
**Objective:** Record pre-change gate state.
**Files:** Modify: none (read-only).
**Step 1:** Run `npm run test` in `/Users/irfan/Documents/Portfolio Redo/personal_website`, record output.
**Step 2:** Run `git status --short && git branch --show-current`, record.
**Verify:** Baseline log saved in plan execution thread. Expected: test passes (or note failures as known).

### Task 2: Router regroup (labels only) ✓ done
**Objective:** New nav groups without deleting pages yet.
**Files:** Modify: `src/ds/ds.js:11-20`.
**Step 1:** Change groups to Start / Foundations / Components / Functions; keep all existing routes rendering (Tokens → mark "Contract · merging into Foundations" via crumb text only).
**Step 2:** Run `npm run test`. Expected: PASS.
**Step 3:** Commit `git commit -m "chore(ds): regroup nav toward target IA"`.

## Phase 1 — Shared infra (tabs + token trace)

### Task 3: `tabs()` helper ✓ done
**Objective:** One tab shell (Preview | Behaviour | Code | Tokens) every component/function reuses.
**Files:** Create: `src/ds/tabs.js` (≤100 lines). Modify: `src/ds/ds.css` (append ≤20 lines, check 100-line budget first).
**Verify:** Unit probe: render tabs with 2 panes in DS, click switches, no console errors. `npm run test` PASS.

### Task 4: `tokenTrace()` helper ✓ done
**Objective:** Single renderer for ramp → semantic → usage rows (kills per-page duplication).
**Files:** Modify: `src/ds/specimens.js` (append export).
**Verify:** Storybook-style check: trace table renders for 2 sample tokens with copy buttons working. `npm run test` PASS.

## Phase 2 — Foundations (one sheet per foundation)

### Task 5: Foundations/Color exemplar ✓ done
**Objective:** Color page shows (a) stone+vermilion ramps ref-only, (b) semantic map with live light/dark probe, (c) usage column naming consuming components.
**Files:** Modify: `src/ds/pages-foundations.js`; Delete: nothing yet; later fold `src/ds/pages-tokens.js` into redirect.
**Verify:** Click swatch copies token; toggle theme top-right updates live values; no component-specific tokens introduced. `npm run test` PASS. ✓ verified — `tokenTrace` renders ramp + semantic rows with usage + live data-live + copy via specimens.js; wip foundations uses tokenTrace, npm test green.
**Done:** 2026-09-09 — foundations Color now via `tokenTrace({ ramp, rows })` with usage column, live probe.

### Task 6: Fold Tokens page ✓ done
**Objective:** `#/tokens` becomes contract index linking into each Foundation page.
**Files:** Modify: `src/ds/pages-tokens.js` (replace table with links + audit state).
**Verify:** No broken hash links from footnav. `npm run test` PASS. ✓ verified — pages-tokens.js is contract index linking to #/foundations per foundation, crumb "Contract · merging into Foundations", build green, footnav intact.
**Done:** 2026-09-09 — Tokens page is redirect/index, ds.js group Foundations.

### Task 7-9: Type / Space-Layout / Shape+Motion sheets ✓ done
**Objective:** Clone Color trace pattern for remaining foundations (scale → role → specimen).
**Files:** Modify: `src/ds/pages-foundations.js` (extract per-foundation renders; split file if >100 lines → `src/ds/foundations-color.js` etc.).
**Verify per sheet:** Type tester, space scale bars, radius-0 doctrine, motion durations all read live `var()`. `npm run test` PASS after each. ✓ verified — Type adds tokenTrace for leading/tracking, Space adds tokenTrace for layout (--size-wrap/header/tap/measures), Shape tokenTrace for --radius-* (live 0), Motion tokenTrace for --dur-*/--ease-*/--fx-*/--blur-*; all via tokenTrace live probe; wc -l 55, npm test green, no split needed.
**Done:** 2026-09-09 — foundations enhanced, all five foundations follow ramp→semantic→usage via tokenTrace, live var() proofs.

## Phase 3 — Components (uniform template + tabs)

### Task 10: Component template + pilot (Button)
**Objective:** Template: Usage → Anatomy → Behaviour/states → tabs(Preview/Code/Tokens) → Playground. Pilot on Button (`pages-primitives.js:4-7`).
**Files:** Modify: `src/ds/pages-primitives.js`.
**Verify:** Button shows all tabs; code copy works; knobs (variant/size/disabled/onDark) drive preview. `npm run test` PASS.

### Task 11: Roll template to Tag/Kicker/Divider/Badge/Field/Toggle/Avatar/Logo
**Objective:** Same template, no new props beyond existing knobs.
**Files:** Modify: `src/ds/pages-primitives.js`, `src/ds/pages-components.js`, `src/ds/pages-library.js` (re-home: primitives→Components/*, landing set stays Library or merges per decision in Task 2).
**Verify:** Each component has Behaviour tab (states incl. dark + focus ring) + Tokens tab. `npm run test` PASS.

### Task 12: Deprecate redundant Components/Landing split
**Objective:** One Components section; old `#/components` static demos either migrate into playgrounds or delete after grep shows 0 importers.
**Files:** Modify: `src/ds/ds.js`, `src/ds/pages-components.js`.
**Verify:** `search_files` for removed exports = 0 hits; `npm run test` PASS; `dist` check.

## Phase 4 — Functions (split + GridReveal as shared function)

### Task 13: Split functions into modules
**Objective:** `pages-functions.js` → `src/ds/functions/grid-reveal.js`, `shimmer.js`, `rise.js`, `viewer.js` + thin index re-exporting.
**Files:** Create: `src/ds/functions/*.js`. Modify: `src/ds/pages-functions.js` (re-export), `src/ds/ds.js` (new hashes `#/functions/grid-reveal` etc.).
**Verify:** All four labs mount; router prev/next walks them. `npm run test` PASS.

### Task 14: GridReveal compact + image test
**Objective:** Lab height capped (~280px), image toggle (helix/fluxx/tas-35) as texture, controls collapsed to cells/morph/span + advanced disclosure.
**Files:** Modify: `src/ds/functions/grid-reveal.js`, `src/ds/fx-lab.js`.
**Verify:** Canvas never pushes controls below fold on 1440px; replay/shuffle work with image on/off; reduced-motion paints one static frame. `npm run test` PASS.

### Task 15: Shared-function section (Grid item + Project hero)
**Objective:** GridReveal page documents single source (`views/masonry/cells.js buildTree`, `gridReveal.js`) + two usage notes: masonry card vs `project.js` case hero.
**Files:** Modify: `src/ds/functions/grid-reveal.js`.
**Verify:** Both usage notes link to live site anchors; no duplicated geometry docs elsewhere.

## Phase 5 — Overview narrative + moodboard (last)

### Task 16: Cyberpunk-editorial narrative ✓ done
**Objective:** Rewrite `pages-overview.js` hero: paper+ink+vermilion, mono-as-machine, 90° cuts; principles unchanged.
**Files:** Modify: `src/ds/pages-overview.js`.
**Verify:** Read-only note intact ("nothing here changes the live site"); `npm run test` PASS. ✓ verified — hero now `Paper. Ink. One vermilion.` + lede with stone paper/deep ink/90° cuts/one vermilion/mono-as-machine/zero radius; principles untouched; read-only note preserved; npm test green.
**Done:** 2026-09-09 — overview hero rewritten via token-annotated lede, status badges retained.

### Task 17: Moodboard strip ✓ done
**Objective:** 4-6 tiles with source link + "what we steal" line; pure CSS grid, images from `/images`+`/icons` or hotlinked refs only.
**Files:** Modify: `src/ds/pages-overview.js`, `src/ds/ds.css` (budget check).
**Verify:** No new deps; tiles proof in light+dark; `npm run test` PASS. ✓ verified — 5 tiles (Cereal/Kinfolk, Müller-Brockmann, Blade Runner 2049, Dieter Rams/Braun, Linear) with source link + steal line, ds-grid c3, /images textures, var()-only inline, light/dark via tokens; ds.css 100→100 (reused grid, no new sheet lines, budget check passed); npm test green.
**Done:** 2026-09-09 — moodboard 5× ds-cell + ds-grid, pure CSS, no new deps.

## Tests / validation (every phase)

- `npm run test` (lint:tokens + vite build) must pass before commit.
- Token hygiene: `search_files` for `#[0-9a-f]{3,6}` outside `tokens.css` + `src/ds` docs = only intentional.
- Line budget: each touched stylesheet/DS module ≤100 lines (split before exceeding).
- Manual: theme toggle proofs every new specimen; reduced-motion spot-check on Functions.

## Risks / tradeoffs / open questions

- Risk: Foundations split balloons file count → mitigate by extracting only when a module nears 100 lines.
- Tradeoff: Tabs add a click to reach code → accepted; Preview-first default keeps scan speed.
- Open: (1) Keep `#/library` as separate Landing section or merge into Components? Recommend merge. (2) Moodboard: hotlink refs vs local copies? Recommend hotlink + citation to avoid weight. (3) Delete `#/tokens` outright or keep as redirect? Recommend redirect for one release.
