# Lump-sum builds — shipments with no scoped plan

> **For Hermes:** the standing catch-all. A shipment no scoped plan covers appends one `### Task N: …` under the phase it belongs to (or opens a new phase) and closes with `*Shipped in <sha> · Tasks a–b · phase-N.*`.

**Goal:** Nothing ships untraceable — every push lands on a scoped plan or here, with its sha recorded next to what changed.

**Architecture:** One running plan file; each phase batches related shipments and each task records what changed plus the sha that proves it, so `git log`, `.hermes/plan-links.json` and `/ds/#/changelog` agree with each other.

**Tech Stack:** Vanilla JS + Vite, portfolio surface only (`main.js`, `src/views`, `src/styles`, `index.html`). Work inside `src/ds` gets a scoped plan of its own.

**Tags:** Layout, Motion, Component, Tooling

---

## Phase 1 — Post-ship mobile + media fixes {#phase-1}

*Tags: Layout*

*Five follow-up commits landed after the mobile-optimisation build closed, with no plan to carry them.*

### Task 1: Gear sits at the end of the icon row ✓ done

**Objective:** Stop the settings gear being fixed while the thumbnail strip scrolls.
**Files:** `src/main.js`, `src/styles/base.css`
**Verify:** Mobile — scrolling the strip leaves the gear in flow at the row end.

### Task 2: Full-bleed rows without the padding clip ✓ done

**Objective:** Remove the left/right padding that clipped full-bleed rows.
**Files:** `src/styles/masonry.css`
**Verify:** Grid rows span edge-to-edge.

### Task 3: Restore the grid gutter, keep the header full-bleed ✓ done

**Objective:** Grid keeps its left/right gutter while the header stays full-bleed.
**Files:** `src/styles/masonry.css`
**Verify:** Gutter visible both sides; header unchanged.

### Task 4: Mobile wrap gutter 16→12 ✓ done

**Objective:** Match the page gutter to the grid gap at ≤640px.
**Files:** `src/styles/masonry.css`
**Verify:** Card gap and page gutter read as one measure.

### Task 5: Responsive images + reveal flicker ✓ done

**Objective:** webp/jpeg srcsets, decode-await, smooth crossfade — kill the mobile reveal flicker.
**Files:** `src/views/img-helpers.js`, `src/views/masonry/cards.js`, `src/views/project.js`
**Verify:** No flash of un-decoded art on mobile load; `npm run smoke` green.

*Shipped in 96f28d6 · Tasks 1–5 · phase-1 — 0c80ab8, e494169, 8e440b7, a330be7 precede it.*

## Phase 2 — Header snap + title pace {#phase-2}

*Tags: Motion*

### Task 6: Strip icon press snap ✓ done

**Objective:** Clicking a strip icon (project thumbs, contact, list/grid) plays the `load.wav` snap through a new `click` sound slot; the loading bed stays `loading.mp3`.
**Files:** `src/views/strip-sound.js`, `src/views/sound-source.js`, `src/views/slot-sound.js`
**Verify:** Every strip icon snaps on click; verifies with sound on in a real browser.

### Task 7: Title scramble 200ms, first visit only ✓ done

**Objective:** Page-title scramble 900ms → 200ms and only on the first visit to a route per session; repeat visits render instantly with no loading sound.
**Files:** `src/views/title-reveal.js`, `src/views/init-sound-load.js`
**Verify:** Measured 212ms scramble on first visit; second visit produced zero scramble frames.

### Task 8: Map regenerated for the committed tree ✓ done

**Objective:** `docs/graph.mmd` had captured nodes from a working tree holding uncommitted modules; regenerate against committed source.
**Files:** `docs/graph.mmd`
**Verify:** Clean checkout — `node scripts/map-graph.mjs` leaves the file unmodified.

*Shipped in 05ec5b2 · Tasks 6–7 · phase-2 — map fix in 8d0fdc5.*

## Phase 3 — Ship rule: no untraceable push {#phase-3}

*Tags: Tooling*

### Task 9: Name + link this lump sum ✓ done

**Objective:** Give this plan its `changelog-names.json` entry and map the seven plan-less shas to their phases in `.hermes/plan-links.json`.
**Files:** `src/ds/changelog-names.json`, `.hermes/plan-links.json`
**Verify:** `npm run plan:names` reports 0 missing; `npm run ds:track` reports tagged = phased.

### Task 10: Standing ship rule in the repo docs ✓ done

**Objective:** `.cursorrules` §5, `AGENTS.md` §5 and README "DS conventions" state the rule — no push without a plan: reuse the covering plan's trailer, else append a task here.
**Files:** `.cursorrules`, `AGENTS.md`, `README.md`
**Verify:** The three files state the same rule; push-time report lists pushed commits and leftover wip.

### Task 11: Retro-link the atlas phase-4.5 planning commit ✓ done

**Objective:** `b5b29ce` cited `#phase-4-5`, an anchor no phase body carries, so it sat in "Unlinked DS changes" — re-point it at the atlas build's phase-5 (dock polish) via `.hermes/plan-links.json`.
**Files:** `.hermes/plan-links.json`, `src/ds/changelog-manifest.json`
**Verify:** `npm run ds:track` → unlinked 0; `/ds/#/changelog` reads "All tracked — every DS commit cites its plan".

### Task 12: Deployed archive reads clean ✓ done

**Objective:** The deployed changelog showed two phantoms — an unlinked plan-mark chore (`2e1b95b`) and `M src/ds/arch-schema.json` under "Uncommitted". Retro-link the chore to the mobile build's phase-4, and stop ds-track reporting the two build-generated DS files (manifest, regenerated schema) as wip.
**Files:** `scripts/ds-track.mjs`, `.hermes/plan-links.json`
**Verify:** Retro links apply after the prev-merge (a merged entry gets linked too); the deployed `/ds/#/changelog` shows no unlinked or uncommitted lines.

### Task 13: Runtime DB stays out of git ✓ done

**Objective:** `.hermes/sessions.db` (empty Hermes runtime file) showed as untracked noise in every status; ignore it like the other local artifacts.
**Files:** `.gitignore`
**Verify:** `git status --short` lists no `.hermes/sessions.db`.

### Task 14: Tracker stops dirtying the tree ✓ done

**Objective:** `npm run dev`/`build` rewrote the manifest with a fresh timestamp even when nothing changed, so `git status` always showed `M src/ds/changelog-manifest.json` — read as "uncommitted code". ds-track now keeps the previous `generated` stamp when `continuation/plans/commits/wip` are unchanged, so an unchanged run leaves the file byte-identical.
**Files:** `scripts/ds-track.mjs`
**Verify:** two consecutive `npm run ds:track` runs → identical checksum; `git status --short` clean.

### Task 15: Generated DS files never dirty the tree ✓ done

**Objective:** `docs/graph.mmd` regenerates regardless, but the two generated DS files trailed every build and every commit: `arch-schema.json` restamped `generated` on each `map-graph` run, and a manifest-only refresh commit re-entered the log, so the manifest could never match its committed self. Both now behave: the schema keeps its stamp when `nodes`/`edges` are unchanged, and a commit whose only DS changes are the two generated files is treated as plumbing and excluded from the log.
**Files:** `scripts/ds-track.mjs`, `scripts/map-graph.mjs`
**Verify:** `npm run ds:track` + `node scripts/map-graph.mjs` twice in a row → identical checksums and a clean `git status`.

*Shipped in 9a82f0f · Tasks 9–15 · phase-3.*

## Phase 4 — Sound polish: hero + reel + header slot {#phase-4}

*Tags: Motion*

### Task 16: Hero window-open on desktop ✓ done

**Objective:** Every hero mosaic plays `window-open` at 0.12 on desktop too.
**Files:** `src/views/project.js`, `src/views/sound-source.js`
**Verify:** Desktop hero reveal emits window-open; mobile unchanged.

### Task 17: Desktop hero de-overlap + second-load silence ✓ done

**Objective:** Loading bed and window-open no longer overlap; second visit to same hero is silent.
**Files:** `src/views/init-sound-load.js`, `src/views/project.js`
**Verify:** No double sound on first load; revisit is silent.

### Task 18: Hero window-open 620→480ms ✓ done

**Objective:** Tighten desktop hero coupling from 620ms to 480ms.
**Files:** `src/views/project.js`
**Verify:** Window-open fires 480ms after grid reveal completes.

### Task 19: Mobile reel linger ✓ done

**Objective:** Card highlight pulls 0.34 with linger; grid reveal stays silent on reel.
**Files:** `src/views/masonry/cards.js`, `src/views/slot-sound.js`
**Verify:** Reel highlight scrambles with pull 0.34; auto grid reveal is silent.

### Task 20: Master volume × per-sound gain ✓ done

**Objective:** Master volume slider drives per-sound gain; faint visitor default, mobile scramble lingers correctly.
**Files:** `src/views/init-sound.js`, `src/views/slot-sound.js`, `src/views/sound-source.js`
**Verify:** Slider attenuates all slots; defaults are faint but audible.

### Task 21: Header sound outside gear pop ✓ done

**Objective:** Sound toggle lives outside the gear dropdown — visible on desktop, dropdown not clipped.
**Files:** `src/main.js`, `src/views/init-sound.js`
**Verify:** Desktop shows sound switch beside strip; gear pop no longer clips.

### Task 22: Header slot fix — soundSwitch to #soundSlot ✓ done

**Objective:** Follow-up to Task 21 — move switch insertion to `#soundSlot`, clean legacy header-inserted switches.
**Files:** `src/main.js`, `src/views/init-sound.js`
**Verify:** Switch renders in `#soundSlot` only; direct-header `.sound-switch` nodes are removed on ensure.

*Shipped in b162ab6 · Tasks 16–22 · phase-4.*

## Phase 5 — Reel shutter: interruptible zing {#phase-5}

*Tags: Motion*

### Task 23: Zing reel stops previous, 45ms shutter ✓ done

**Objective:** Mobile card highlight `zing.mp3` was gated at 380ms and queued behind the file — fast scroll sounded erratic. New `reel-tick.js` kills the active `Audio` before firing next, throttle 380→45ms, so each card ticks and previous is cut like a shutter reel.
**Files:** `src/views/masonry/reel-tick.js`, `src/views/masonry/reveal.js`, `docs/graph.mmd`, `src/ds/arch-schema.json`
**Verify:** Fast scroll on ≤640px emits a tick per card with previous cut; slow scroll still ticks once per highlight; dev server `http://192.168.0.104:5173` audible.

*Shipped in e2252fd · Tasks 23–23 · phase-5.*

## Phase 6 — Loading sound sync + hero height cache {#phase-6}

*Tags: Motion*

*One commit pairs the loading bed with the first scramble and caches hero height per tab.*

| # | Task | Done when |
|---|------|-----------|
| 24 | Loading sound follows first scramble, hero height cached | Loading bed fires with first scramble; hero height cached per tab |

### Task 24: Loading sound follows first scramble, hero height cached ✓ done

*2026-09-15 19:35 · 08d6e66 · feat(portfolio): sync loading sound to first scramble and cache hero height per tab*

**Objective:** Loading bed fires with the first title scramble, not before; hero height cached per tab so revisit lays out instantly.
**Files:** `src/views/init-sound-load.js`, `src/views/hero-height-cache.js`, `src/views/title-reveal.js`, `src/views/rise.js`
**Verify:** First visit plays the loading bed with the scramble; revisit is silent with hero height intact.

*Shipped in 08d6e66 · Tasks 24–24 · phase-6.*

## Phase 7 — Settings surface: sheet, modal, anchored dropdown {#phase-7}

*Tags: Component*

*Nine commits hunting the settings home — footer morph, bottom sheet, centered modal, anchored dropdown.*

| # | Task | Done when |
|---|------|-----------|
| 25 | Modern footer, mobile settings + morphing close | Footer modern; morphing close on mobile |
| 26 | Restore settings sheet, hero title to footer | Sheet restored; title in footer |
| 27 | Pill-height settings, dropdown anchored under gear | Control matches pill; dropdown under gear |
| 28 | Gear matches pill height | Heights align exactly |
| 29 | Bottom sheet back, near gear on mobile list | Sheet near gear on mobile |
| 30 | Dropdown via fWrap absolute, escapes backdrop-filter | Unclipped over header |
| 31 | Settings as centered modal + backdrop overlay | Centered modal with scrim |
| 32 | Mobile-only modal, full-width volume, keep frame | Mobile volume spans modal |
| 33 | Outside-close on desktop, volume full-width in mobile modal | Outside click closes; mobile volume full-width |

### Task 25: Modern footer, mobile settings + morphing close ✓ done

*2026-09-16 12:41 · 031395c · feat(portfolio): modern footer, mobile settings + morphing close*

**Objective:** Footer goes modern; mobile settings gain a morphing close control.
**Files:** `src/main.js`, `src/views/stripbar.js`, `src/views/settings-menu.js`, `src/styles/pages.css`
**Verify:** Footer renders modern; mobile settings open and morph closed.

### Task 26: Restore settings sheet, hero title to footer ✓ done

*2026-09-16 12:54 · 3299e52 · fix(portfolio): restore settings sheet + move hero title to footer*

**Objective:** Bring back the settings sheet; hero title moves down to the footer.
**Files:** `src/views/masonry.js`, `src/styles/pages.css`
**Verify:** Sheet opens from gear; hero title sits in the footer.

### Task 27: Pill-height settings, dropdown anchored under gear ✓ done

*2026-09-16 12:58 · 6d9fdd1 · fix(portfolio): pill-height settings + anchored dropdown under gear*

**Objective:** Settings control matches pill height; dropdown anchors under the gear.
**Files:** `src/views/settings-menu.js`, `src/main.js`, `src/styles/base-actions.css`, `src/styles/pages.css`
**Verify:** Gear and pill share height; dropdown opens beneath the gear.

### Task 28: Gear matches pill height ✓ done

*2026-09-16 13:01 · bf62cf7 · fix(portfolio): gear matches pill height + dropdown anchored*

**Objective:** Follow-up alignment — gear height tracks the pill exactly.
**Files:** `src/styles/base-actions.css`, `src/styles/pages.css`
**Verify:** Gear and pill heights match at all widths.

### Task 29: Bottom sheet back, near gear on mobile list ✓ done

*2026-09-16 13:06 · 6ed4848 · fix(portfolio): restore bottom sheet + position near gear on mobile list*

**Objective:** Restore the bottom sheet; on mobile list it positions near the gear.
**Files:** `src/views/settings-menu.js`
**Verify:** Mobile list shows the sheet anchored near the gear.

### Task 30: Dropdown via fWrap absolute, escapes header backdrop-filter ✓ done

*2026-09-16 13:08 · 77e8757 · fix(portfolio): dropdown via fWrap absolute, escape header backdrop-filter*

**Objective:** Dropdown positions via fWrap absolute so the header backdrop-filter no longer clips it.
**Files:** `src/views/settings-menu.js`
**Verify:** Dropdown opens unclipped over the header filter.

### Task 31: Settings as centered modal + backdrop overlay ✓ done

*2026-09-16 13:10 · bf97af5 · feat(portfolio): settings as centered modal + backdrop overlay*

**Objective:** Settings become a centered modal with a backdrop overlay.
**Files:** `src/views/settings-menu.js`, `src/styles/base.css`, `src/styles/pages.css`
**Verify:** Settings open centered with dimmed backdrop; scrim closes.

### Task 32: Mobile-only modal, full-width volume, keep frame ✓ done

*2026-09-16 13:14 · 66c9ced · fix(portfolio): mobile modal only, full-width volume, no right pad, keep frame*

**Objective:** Modal treatment applies on mobile only; volume goes full-width with no right pad, frame kept.
**Files:** `src/views/settings-menu.js`, `src/views/init-sound-vol.js`, `src/styles/base.css`
**Verify:** Desktop unchanged; mobile modal shows full-width volume.

### Task 33: Outside-close on desktop, volume full-width in mobile modal ✓ done

*2026-09-16 13:18 · 73db667 · fix(portfolio): outside close desktop, volume in mobile modal full width*

**Objective:** Desktop modal closes on outside click; mobile modal keeps full-width volume.
**Files:** `src/views/settings-menu.js`, `src/views/init-sound.js`
**Verify:** Desktop outside click closes; mobile volume spans the modal.

*Shipped in 73db667 · Tasks 25–33 · phase-7 — 031395c, 3299e52, 6d9fdd1, bf62cf7, 6ed4848, 77e8757, bf97af5, 66c9ced precede it.*

## Phase 8 — Reel zing gating: first card to per-card ready {#phase-8}

*Tags: Motion*

*Four increasingly tight gates on the mobile grid zing so scrolling ticks only over revealed cards.*

| # | Task | Done when |
|---|------|-----------|
| 34 | Mobile grid zing waits for first card reveal | Silent until first reveal |
| 35 | Zing waits until all grid cards ready | Holds until full grid ready |
| 36 | Zing after first fold of 6 cards ready | Starts at 6-card fold |
| 37 | Per-card zing on card.ready plus first-fold | Ready cards tick past fold |

### Task 34: Mobile grid zing waits for first card reveal ✓ done

*2026-09-16 13:21 · 3225bf7 · fix(sound): gate mobile grid zing until first card reveal ready*

**Objective:** Grid zing stays silent until the first card reveal is ready.
**Files:** `src/views/masonry/reel-tick.js`
**Verify:** No zing before first reveal; ticks start after.

### Task 35: Zing waits until all grid cards ready ✓ done

*2026-09-16 13:30 · acb1bdd · fix(sound): gate zing until all grid cards ready*

**Objective:** Tighten the gate — zing holds until every grid card is ready.
**Files:** `src/views/masonry/reel-tick.js`
**Verify:** Early scroll is silent; full grid ticks.

### Task 36: Zing after first fold of 6 cards ready ✓ done

*2026-09-16 13:31 · 11e9955 · fix(sound): zing after first fold (6 cards) ready*

**Objective:** Relax to first-fold readiness — six cards suffice for the zing to start.
**Files:** `src/views/masonry/reel-tick.js`
**Verify:** Zing begins once the first fold is ready.

### Task 37: Per-card zing on card.ready plus first-fold ✓ done

*2026-09-16 13:34 · f91c68f · fix(sound): per-card zing gated on card.ready + first-fold*

**Objective:** Each card zings only when itself ready, and only past the first fold.
**Files:** `src/views/masonry/reel-tick.js`, `src/views/masonry/reveal.js`
**Verify:** Unready cards stay silent; ready cards tick past the fold.

*Shipped in f91c68f · Tasks 34–37 · phase-8 — 3225bf7, acb1bdd, 11e9955 precede it.*

## Phase 9 — Proceed gate: cyberpunk authorize, subtle redux {#phase-9}

*Tags: Component*

*Entry gate arrives loud then leaves quiet — cyberpunk authorize preview first, glimmer-orb redux after.*

| # | Task | Done when |
|---|------|-----------|
| 38 | Cyberpunk proceed gate + live authorize preview on volume seek | Gate with live preview |
| 39 | Subtle redux — glimmer orb + dotted corners | Orb glimmers; corners dotted |

### Task 38: Cyberpunk proceed gate + live authorize preview on volume seek ✓ done

*2026-09-16 13:42 · 05e9ab5 · feat(portfolio): cyberpunk proceed gate + live authorize preview on volume seek*

**Objective:** New proceed gate with live authorize preview that reacts to volume seek.
**Files:** `src/views/proceed-gate.js`, `src/styles/proceed.css`, `src/views/init-sound.js`, `index.html`
**Verify:** Gate authorizes with cyberpunk styling; seeking volume previews live.

### Task 39: Subtle redux — glimmer orb + dotted corners ✓ done

*2026-09-16 13:46 · e3db2c1 · feat(proceed): subtle redux gate with glimmer orb + dotted corners*

**Objective:** Tone the gate down — glimmer orb and dotted corners, less neon.
**Files:** `src/views/proceed-gate.js`, `src/styles/proceed.css`
**Verify:** Gate reads subtle; orb glimmers, corners dotted.

*Shipped in e3db2c1 · Tasks 38–39 · phase-9 — 05e9ab5 precedes it.*

## Phase 10 — Token + changelog: gate vars, heat & badge times {#phase-10}

*Tags: Tooling*

*The proceed gate shipped with raw px (18 violations) and the changelog's heat/badges hid portfolio work — this pass tokenizes the gate and teaches the tracker/calendar to show every retro-linked sha with `date time`.*

| # | Task | Done when |
|---|------|-----------|
| 40 | Gate and menu use token vars + changelog heat/badge times | `lint:tokens` clean; calendar heat + badges show `date time` for every linked sha |

### Task 40: Gate and menu use token vars, no raw px ✓ done

*2026-09-16 13:59 · a8199b3 · chore(lump-sum): phases 6-10 — retro 16 shas + gate token compliance*

**Objective:** Proceed gate, base menu pop, the tokens they need, and the changelog (calendar heat + per-task badges with time) all use var()/tracked commits; lint:tokens passes 38+33 clean.
**Files:** `src/styles/proceed.css`, `src/styles/base.css`, `src/styles/tokens-type.css`, `src/ds/changelog-links.js`, `scripts/ds-track.mjs`
**Verify:** `npm run lint:tokens` → ✓ clean; `npm test` → build succeeds; `node scripts/ds-track.mjs` → 222/222 linked · Sep 16 heat shows 10+ commits · badges show `date time`.

*Shipped in a8199b3 · Tasks 40–40 · phase-10.*

## Phase 11 — Visitor volume + desktop hover zing {#phase-11}

*Tags: Motion*

*Two small sound-surface tweaks — quieter first visit and desktop zing on hover instead of only mobile viewport.*

| # | Task | Done when |
|---|------|-----------|
| 41 | New-visitor volume 0.44 | `getVolume()` returns 0.44 when no `adam-volume` key |
| 42 | Desktop zing on mouseenter | Cards `mouseenter` above 640px plays `zing` via `desktopZing` |

### Task 41: New-visitor volume 0.44 ✓ done

*2026-09-16 14:22 · 7a56269 · feat(sound): visitor volume 0.44 + desktop hover zing*

**Objective:** New visitors hear everything at 0.44 master volume instead of full gain.
**Files:** `src/views/audio-ctx.js`
**Verify:** Fresh profile (clear `adam-volume`) → slider reads 44%; `playFileId` gains scaled by `scaledGain(×0.44)`.

### Task 42: Desktop zing on mouseenter ✓ done

*2026-09-16 14:22 · 7a56269 · feat(sound): visitor volume 0.44 + desktop hover zing*

**Objective:** Desktop (>640, hover) cards zing on `mouseenter` instead of only the mobile viewport linger; mobile keeps its `IntersectionObserver` reel.
**Files:** `src/views/masonry/reel-tick.js`, `src/views/masonry.js`
**Verify:** Desktop hover over a `ready` card past first fold → `zing.mp3` at 0.2×volume with shutter cut; mobile viewport zing unchanged.

*Shipped in 7a56269 · Tasks 41–42 · phase-11.*

## Phase 12 — Visitor volume 0.36 + footer shimmer {#phase-12}

*Tags: Motion*

*First-take volume at 0.44 felt hot; dial to 0.36, and give the `Working on stories that last` footer a muted shimmer clipped to the glyphs that makes the letters bloom where the cursor is.*

| # | Task | Done when |
|---|------|-----------|
| 43 | New-visitor volume 0.44 → 0.36 | `getVolume()` returns 0.36 with no key |
| 44 | Footer shimmer + mouse glow | `.footer-big` shimmers and glows at `var(--mx)`/`--my` |

### Task 43: New-visitor volume 0.44 → 0.36 ✓ done

*2026-09-16 14:26 · 223badb · feat(portfolio): visitor volume 0.36 + footer shimmer glow*

**Objective:** Lower the cold-start master volume from 0.44 to 0.36.
**Files:** `src/views/audio-ctx.js`
**Verify:** Clear `adam-volume` → slider 36% on reload; gains scaled ×0.36.

### Task 44: Footer shimmer + mouse glow ✓ done

*2026-09-16 14:26 · 223badb · feat(portfolio): visitor volume 0.36 + footer shimmer glow*

**Objective:** `.footer-big` rests more muted (`opacity 0.48`, `var(--color-ink-muted)`, no glow/shimmer), and on hover the letters themselves glow — radial `var(--mx)`/`--my` clipped to text with shimmer, paused when not hovering.
**Files:** `src/styles/base-atoms.css`, `src/views/footer-shimmer.js`, `src/main.js`
**Verify:** Footer text more muted at rest without glow; moving the mouse blooms the letters (not the box) at the cursor; `prefers-reduced-motion` disables both.

*Shipped in 223badb · Tasks 43–44 · phase-12.*

## Phase 13 — Footer fidelity to the original Framer footer {#phase-13}

*Tags: Layout*

*The reference footer reads: sans link grid (left- and right-aligned pairs), hairline divider, dark mono podcast note — and on the right the display title at `--color-ink-subtle` low opacity with the copyright pinned to the bottom under its own faint rule, inside the dashed corner-marked frame at a generous gutter.*

| # | Task | Done when |
|---|------|-----------|
| 45 | Extract footer into `styles/footer.css` + wire `index.html` | `base-atoms.css` ≤100 lines; footer.css linked after it |
| 46 | Match original: sans links, darker note, lighter title, © bottom-right col, 32 pad, 112 gutter | Footer renders per reference at `/` |

### Task 45: Footer stylesheet extraction ✓ done

*2026-09-17 12:10 · 0737aec · style(footer): match original framer footer — sans links, faint title, © under right col*

**Objective:** Move all footer rules out of `base-atoms.css` into `src/styles/footer.css` and link it in `index.html` right after `base-atoms.css`.
**Files:** `src/styles/footer.css`, `src/styles/base-atoms.css`, `index.html`
**Verify:** `npm run lint:tokens` clean; footer renders identically before the Task 46 restyle.

### Task 46: Reference match — links, note, title tone, © placement ✓ done

*2026-09-17 12:10 · 0737aec · style(footer): match original framer footer — sans links, faint title, © under right col*

**Objective:** Links → `--font-sans` `--text-h3` `--weight-medium`; note → `--color-ink` @ 0.78 mono; big title → `--color-ink-subtle` @ 0.35 (bloom unchanged); © → bottom of the right column under a `--border-hairline` rule, `--space-32` frame padding, `--space-112` gutter, `--space-420` min-height.
**Files:** `src/styles/footer.css`, `src/views/shared.js`
**Verify:** At `/` the footer matches the reference: sans links, divider, dark mono note left; faint heavy title right with © pinned bottom; dashed frame + corner handles intact.

*Shipped in 0737aec · Tasks 45–46 · phase-13.*

## Phase 14 — Retire the invisible-commit gap {#phase-14}

*Tags: Tooling*

*33 commits shipped since 9/15 sat outside the tracker — footer/volume phases 11–13 and the mobile optimisation touches `src/styles/*` & `src/views/*`, which aren't DS paths, so `/ds/#/changelog` never saw them. Every one gets retro-linked through `plan-links.json` (the sanctioned no-rewrite channel) with its true plan + phase anchor, so task badges and calendar heat resolve.*

| # | Task | Done when |
|---|------|-----------|
| 47 | Retro-link the 33 straggler shas (mobile P1–P4, lump-sum P5, P10–P13) | `ds-track` → 256/256 linked, 0 wip |

### Task 47: Retro-link the 33 straggler shas ✓ done

*2026-09-17 12:16 · 97dd81b · chore(ds): retro-link 33 straggler shas — footer/volume + mobile phases now tracked*

**Objective:** Map every manifest-missing commit since 9/15 into `.hermes/plan-links.json` — trailer parsed from the commit's own subject where present, mobile-plan closes mapped by hand.
**Files:** `.hermes/plan-links.json`, `src/ds/changelog-manifest.json`
**Verify:** `node scripts/ds-track.mjs` → `256 DS commits (256 linked) · 0 wip`; badges + calendar heat resolve for phases 11–13.

*Shipped in 97dd81b · Tasks 47–47 · phase-14.*

## Phase 15 — Footer fidelity round 2 + strip overflow {#phase-15}

*Tags: Layout*

*Pixel-measured both screenshots (Pillow @2x): the design's corner squares are 4 CSS px nubs sitting INSIDE the corner with 4–5 px pale dashes — mine were 12 px straddling the frame edge with #56606d dashes, which also read as "overflow". Links were row-major (Linkedin/Medium/Strava) instead of the design's column-major pairs. And the real overflow: header strip thumbs ran past the viewport at 641–1097 px with no scroll container.*

| # | Task | Done when |
|---|------|-----------|
| 48 | Links column-major, second column right-aligned | `L/D/M` left, `X/S/G` right flush |
| 49 | Pale dashes + 4 px corner nubs inside the frame | Border reads light gray; nubs sit on corners |
| 50 | Strip scrolls at all widths (was clipped 641–1097) | `overflow: auto hidden` + `canScroll` true |

### Task 48: Links column-major, second column right-aligned ✓ done

*2026-09-17 12:41 · 6e59812 · style(footer): pale dashes + 4px corner nubs, links column-major right-aligned, strip scrolls*

**Objective:** `grid-auto-flow: column` + 3 rows so the link columns pair like the design (Linkedin/Dribbble/Medium | X/Strava/Goodreads); second column `justify-self: end`.
**Files:** `src/styles/footer.css`
**Verify:** Column pairing + right-flush edges; measured `r648` on all three right items.

### Task 49: Pale dashes + 4 px nubs inside the corner ✓ done

*2026-09-17 12:41 · 6e59812 · style(footer): pale dashes + 4px corner nubs, links column-major right-aligned, strip scrolls*

**Objective:** Border → `1px dashed color-mix(ink-subtle 52%)`; corners → `--space-5` nubs at `-1px` (inside the frame corner, covering the border), replacing the 12 px straddling squares.
**Files:** `src/styles/footer.css`
**Verify:** Rendered frame reads pale with black corner nubs; no element pokes past the frame.

### Task 50: Strip scroll container at all widths ✓ done

*2026-09-17 12:41 · 6e59812 · style(footer): pale dashes + 4px corner nubs, links column-major right-aligned, strip scrolls*

**Objective:** `.stripbar .strip` gets `overflow: auto hidden` + `scrollbar-width: none` at base (mobile media block now redundant and trimmed); thumbs scroll instead of overrunning the viewport at 641–1097 px.
**Files:** `src/styles/base.css`
**Verify:** At 1000 px: `canScroll=true`, `scrollW 1084 > clientW 912`; no page-level horizontal overflow.

*Shipped in 6e59812 · Tasks 48–50 · phase-15.*

## Phase 16 — Footer title at design scale {#phase-16}

*Tags: Layout*

*The user's "not using flex width" call: the title wrapped into three narrow lines ("Working on / stories / that last") because the markup force-broke at the spans and the cap (132px/11vw) was far above the design's. Measured the design at 1512 with Pillow + browser font metrics: title ≈ 98px, line 1 "Working on stories" = 779px, line 2 "that last" = 312px, title column starts ≈ 675px, left column ends ≈ 600px, frame ≈ 312–330px tall.*

| # | Task | Done when |
|---|------|-----------|
| 51 | Title wraps naturally (single text node) | Break reads "Working on stories / that last" |
| 52 | Font 6.5vw/100 + 40/60 grid + drop min-height | Line 1 ≈ 777px, frame ≈ 344px, no overflow |

### Task 51: Title wraps naturally ✓ done

*2026-09-17 13:02 · 86a1ec7 · style(footer): title at design scale — natural wrap, 6.5vw/100, 40/60 grid*

**Objective:** Replace `<span>Working on</span><span>stories that last</span>` (forced block break) with a single text node; remove the now-unused `.footer-big span` rule and `text-wrap: balance` (balance fights the design's greedy break).
**Files:** `src/views/shared.js`, `src/styles/footer.css`
**Verify:** `getClientRects()` → `["777x119","332x119"]` = "Working on stories" / "that last".

### Task 52: Title at design scale ✓ done

*2026-09-17 13:02 · 86a1ec7 · style(footer): title at design scale — natural wrap, 6.5vw/100, 40/60 grid*

**Objective:** `--text-footer: clamp(36px, 6.5vw, 100px)` (≈98px @1512, solved from browser font metrics vs design's 779px line), grid 40fr/60fr with `--space-70` gutter, remove `min-height: 420` so the frame is content-sized like the design.
**Files:** `src/styles/tokens-type.css`, `src/styles/footer.css`
**Verify:** font 98.28px @1512; title x657 w802; cols H344; left column ends 587 ≈ design 600; `bad=0` overflow.

*Shipped in 86a1ec7 · Tasks 51–52 · phase-16.*

## Phase 17 — Footer owns its padding + dual corner squares {#phase-17}

*Tags: Layout*

*User found the right-side padding gone on project detail pages. Root cause: `case.css` `.wrap:has(> .case) { padding-right: 0; padding-left: var(--space-12) }` (intended full-bleed for the case hero) — the footer lived inside that wrap and inherited the asymmetry (measured L16/R4). Second ask: the design's corner marks are TWO layered squares, which the raw pixel map confirmed (8×8 @ ~14% ink + 4×4 @ ~68% ink nested at the corner), and the dashes read ~17/242 below the interior, not the 52% mix used before.*

| # | Task | Done when |
|---|------|-----------|
| 53 | Footer escapes the host container, owns its padding | `body > footer` on every route; frame insets L20/R20 at 1512 |
| 54 | Dual corner squares + paler dashes | 8px/14% + 4px/68% at each corner; dash mix 24% |

### Task 53: Footer owns its padding ✓ done

*2026-09-17 13:21 · 70520a5 · fix(footer): own padding system outside the wrap + dual corner squares, paler dashes*

**Objective:** `route()` in `main.js` relocates the rendered footer to `body` (removes the previous body footer first); `footer.css` gains `max-width: var(--size-wrap); margin: var(--space-46) auto 0; padding-inline: var(--space-20)` (16px ≤760), plus new `--space-20` token. The footer no longer depends on any parent container's padding.
**Files:** `src/main.js`, `src/styles/footer.css`, `src/styles/tokens-type.css`
**Verify:** project + masonry routes at 1512: `footer.parent=BODY`, inset L=20 R=20, `bad=0`; 700px: L=R=16.

### Task 54: Dual corner squares ✓ done

*2026-09-17 13:21 · 70520a5 · fix(footer): own padding system outside the wrap + dual corner squares, paler dashes*

**Objective:** `.footer-corner` = 8px @ `color-mix(ink 14%)` with `::after` = 4px @ `color-mix(ink 68%)`, both flush `0` at the corner quadrant; dashed border mix 52% → 24% (design delta).
**Files:** `src/styles/footer.css`
**Verify:** computed `cornerTL=8px/0.14` + `::after=4px/0.68`, dash `24%`; vision confirmed two-layer marks at all four corners.

*Shipped in 70520a5 · Tasks 53–54 · phase-17.*

## Phase 18 — Viewer physics + grid viewfinder {#phase-18}

*Tags: Motion, Component*

*Cursor-following hero popover for the header strip and the list view (physics, not timing), plus the masonry grid's desktop viewfinder — corner brackets and a slightly dark scrim that appear only on hover.*

### Task 55: Strip icon hover viewer — physics follower ✓ done

*2026-09-17 14:30 · feat(viewer): strip icon hover hero popover with spring follower*

**Objective:** Header strip icons each show their project's hero in a cursor-following popover — position and scale ride one rAF via `createFollower` (K/FR + pop K/FR), offset ~1.6→0.8px, per-icon `dip()`→`settle()` so every project change pops, desktop `>640px` + `prefers-reduced-motion`/`hover:none` gated.

**Files:** `src/views/strip-viewer.js`, `src/views/strip-viewer-helpers.js`, `src/styles/tokens-fx.css`, `src/styles/viewer.css`, `src/main.js`

**Verify:** Hover strip icons on `>640px` — hero follows cursor, scales 0.985→1 with short overshoot per icon, no lag, no mobile popover; `prefers-reduced-motion` skips scale.

### Task 56: List view hover viewer — same physics follower ✓ done

*2026-09-17 14:35 · feat(viewer): list rows show cursor-following hero popover*

**Objective:** List rows (`works` with `data-hero`) show the same `viewer--strip` popover as the strip — `attachListViewer(grid)` mirrors the strip physics, swaps hero per row with `dip()`→`settle()`, cleans up on view switch/unmount.

**Files:** `src/views/masonry/list-viewer.js`, `src/views/masonry.js`, `src/views/masonry/cards.js`

**Verify:** At `#/masonry` list view `>640px` — moving between rows pops the hero per row; `≤640px` no popover; switching back to grid cleans the portal.

### Task 57: Physics tuning — less travel, less bounce ✓ done

*2026-09-17 14:50 · chore(viewer): tighten follow — less translation, damped bounce*

**Objective:** Reduce the viewer's extra translation 80% then another half (GAP `8→0.8px`, scale `0.88→0.985`), raise follow stiffness `K 0.1→0.32` and `FR 0.54→0.58`, pop `K 0.22→0.28` `FR 0.62→0.55` — follower now tracks cursor with weight but settles without jiggle, per-project `dip` still reads.

**Files:** `src/styles/tokens-fx.css`, `src/views/strip-viewer.js`, `src/views/masonry/list-viewer.js`, `src/views/strip-viewer-helpers.js`

**Verify:** Fast cursor moves right — no lag trail; slow moves settle softly; per-icon pop still overshoots once.

### Task 58: Grid viewfinder — brackets + dim, desktop hover only ✓ done

*2026-09-17 15:00 · feat(card): grid cards show corner brackets + dim on desktop hover*

**Objective:** Masonry cards show a viewfinder on desktop hover only: four `2px` `var(--color-viewer-line)` L's at `var(--space-8)` inset (`var(--space-16)` square) and a full-bleed `card-dim` (`color-mix(var(--color-viewer-shade) 42%)`) over the image, plus the existing bottom `scrim`/`card-info`; hidden on `≤640px` or `hover:none`, faded via `var(--dur-fast)` and token colors only.

**Files:** `src/views/masonry/cards.js`, `src/styles/card.css`, `src/styles/card-info.css`

**Verify:** Desktop `>640px` hover a grid card — white corners + slight dark wash + title scrim appear; move off — fade out; mobile/touch shows no brackets/dim, `in-view` scrim only.

*Shipped in 4f7de17 · Tasks 55–58 · phase-18.*

## Phase 19 — List view preview pane {#phase-19}

*Tags: Layout, Component*

*List view gains a right-hand project pane (sticky, promo-card crop) while the cursor popover stays, and both list previews now show the masonry card image instead of the `/heroes/*` detail composite.*

### Task 59: List view preview pane — right column, promo crop ✓ done

*2026-09-18 15:25 · feat(list): right preview pane alongside rows + hover swap*

**Objective:** List view splits into `.works-cols` — rows column + sticky `.peek` pane sized `--size-peek-w` (clamp `--space-160` → `--space-360`) — seeded with the first project's promo crop by `peekMarkup` and swapped on row hover through a `--dur-viewer-swap` opacity dip in `attachWorkPeek`; `attachListView` returns one teardown covering both the pane and the unchanged cursor popover; pane hidden `≤640px`.

**Files:** `src/views/masonry/list-view.js`, `src/views/masonry/work-peek.js`, `src/views/masonry.js`, `src/styles/pages-sections.css`, `src/styles/tokens-fx.css`

**Verify:** 1440px list view — pane 346px at the rows' right, seeded `tas-51-800.webp`, sticky; hover a row → pane dips to that project; width sweep 761→2244 → 0 horizontal overflow, mobile 390 hides the pane and collapses to one column; filter redraws keep the pane; no console exceptions.

### Task 60: List previews show the promo card crop ✓ done

*2026-09-18 15:25 · feat(list): previews read the card crop, not the detail hero*

**Objective:** Rows carry `data-card` (480w webp for the popover) + `data-peek` (nearest 800w for the pane) resolved by a new nearest-width `cardSrc()` picker, so both list previews render the masonry card image `/images/<slug>-N.webp` instead of the `/heroes/*` device-composite; `data-hero` retired.

**Files:** `src/views/img-helpers.js`, `src/views/masonry/cards.js`, `src/views/masonry/list-viewer.js`

**Verify:** Hover rows — popover and pane both show `/images/<slug>-N.webp`; `grep data-hero` is empty outside plans; grid view untouched (cards still `/images/*` srcset + mock hover).

*Shipped in 5fbfb1a · Tasks 59–60 · phase-19.*

## Phase 20 — List pane previews the project page, not the card crop {#phase-20}

*Tags: Layout, Component*

*The pane stops repeating the grid crop: it is now the detail page's media box — same aspect, cover crop and GridReveal mosaic — so hovering a row previews the project's own page, while the cursor popover keeps the card crop.*

### Task 61: Pane = detail hero-box with mosaic swap ✓ done

*2026-09-18 15:55 · feat(list): pane previews the project's detail media*

**Objective:** `peekMarkup` renders `aside.peek.case-media.hero-box` — the detail media treatment (hairline border, panel bg, `--hero-aspect` from the project tuple, cover crop, `.gr` canvas, `.ready` gating) — fed by `hero || mock || image` at the nearest 800w; `attachWorkPeek` swaps aspect + src + width/height and re-runs `attachGridReveal` on hover so each project mosaics in; rows carry `data-media` + `data-w/h` (`data-peek` retired).

**Files:** `src/views/masonry/work-peek.js`, `src/views/masonry/cards.js`, `src/styles/pages-sections.css`

**Verify:** 1440px — pane `tas-51` aspect 848/1024, 346×417, `/heroes/tas-51-800.webp`, `.ready`; hover `fluxx` → 683/1024, 346×518, `/heroes/fluxx-800.webp`; popover still the card crop (`/images/*-480.webp`); mobile hides the pane; no console exceptions.

*Shipped in 2d6c1ca · Task 61 · phase-20.*

## Phase 21 — List split matches the detail page {#phase-21}

*Tags: Layout*

*The list columns become `1fr 1fr` with `gap: 0` — rows keep the copy column's gutter padding, the pane fills its whole half — so the list reads at the detail page's proportion instead of a narrow sidebar.*

### Task 62: Equal columns, detail-page proportions ✓ done

*2026-09-18 15:55 · style(list): 1fr/1fr split, pane fills its half*

**Objective:** `.works-cols` becomes the detail page's `.case-grid` split (`1fr 1fr`, `gap: 0`), `.works-rows` takes the same `padding-right: var(--gutter)` the detail copy has, and the pane stretches to its column — the `--size-peek-w` clamp is retired.

**Files:** `src/styles/pages-sections.css`, `src/styles/tokens-fx.css`

**Verify:** 1440px → columns 704/704 against the detail page's 714/714, pane 704×850, rows pad-right 16px; 1180/900/700 → 574/434/334 per column, 0 horizontal overflow; pane still hidden `≤640px`; no console exceptions.

*Shipped in 9375655 · Task 62 · phase-21.*

## Phase 22 — Shared flight into the project page {#phase-22}

*Tags: Motion, Component*

*Entering a project from the list flies the pane's media into the hero's slot — the vertical offset between the two reads as motion instead of a jump — and the arrival re-mosaics instead of settling.*

### Task 63: Shared flight from the list pane into the hero ✓ done

*2026-09-18 16:09 · feat(transit): media flight from the list pane into the hero*

**Objective:** `views/transit.js` arms on a row click whose pane already shows that project (`armTransit`), measures the pane before the render and hides the hero (`takeTransit` → `html.is-transit` + `forgetReveal`), then flies a `.transit-ghost` from the pane's rect to the hero's rect on `--dur-transit` / `--ease-signature` and reveals the hero at landing (`flyTransit`).

**Files:** `src/views/transit.js`, `src/styles/transit.css`, `src/main.js`, `index.html`, `src/styles/tokens-fx.css`

**Verify:** From the list at 1440px — ghost builds at the pane rect (720,151 704×1055), transform animates translateY 0 → −65px + scale 1 → 1.014, hero hidden while `html.is-transit`, ghost gone and hero visible at landing; no flight when the pane is hidden (≤640) or reduced-motion is on; no console exceptions.

### Task 64: Arrival and hover swaps re-mosaic ✓ done

*2026-09-18 16:09 · feat(reveal): replay flag — pane swaps and the flight re-mosaic*

**Objective:** `gateLoad`/`attachGridReveal` take a `replay` flag that skips the seen/complete fast path so the mosaic runs even when the bitmap is already decoded; the pane's hover swaps pass it, and `rise.js` passes it when the hero carries `data-reveal="replay"` (set by the router whenever a flight is armed). `forgetReveal(src)` drops the pane's cache entry so the arrival reveal cannot short-circuit.

**Files:** `src/views/masonry/gridReveal-load.js`, `src/views/masonry/gridReveal.js`, `src/views/masonry/work-peek.js`, `src/views/rise.js`, `src/main.js`

**Verify:** Arrival — canvas pixel samples shift over ~1.2s, `.ready` at ~1.37s then the blur swap (canvas → 0, img → 1); pane hover swap — mosaic ramp with `.ready` at ~1.2s; cached/direct visits and the mobile hero replay keep their old timing.

*Shipped in 6b043a6 · Tasks 63–64 · phase-22.*

## Phase 23 — Single-render popover, calm follower, patient pane {#phase-23}

*Tags: Motion, Component*

*The cursor popover becomes one persistent node whose row changes swap only `img.src` and re-aim — no per-row scale dip — riding a calmer follower (x immediate, y sprung with a dead zone); the right-side pane waits out fast travel and skips the mosaic for already-seen media.*

### Task 65: One popover node + inertia follower ✓ done

*2026-09-20 · feat(hover): popover swaps src-only, follower springs y only*

**Objective:** `attachListViewer`/`attachStripViewer` keep a single `makeStripEl` node for the session — first show snaps + pops, every later row change only swaps `img.src` and `fol.aim`s (the dip/settle scale loop is retired); `createFollower` runs x immediate with y on an inertia spring behind `--fx-viewer-y-dead`, scale on its own spring from 0.985; all row/card imagery preloads, VH stays a fixed 240 (no offsetHeight remeasure, no load-listener re-aim), mousemove is rAF-throttled and passive, and the strip warms decodes; `.viewer--strip` animates transform only.

**Files:** `src/views/masonry/list-viewer.js`, `src/views/strip-viewer.js`, `src/views/strip-viewer-helpers.js`, `src/views/masonry/viewer.js`, `src/views/masonry/viewer-bind.js`, `src/styles/tokens-fx.css`, `src/styles/viewer.css`

**Verify:** Build green; hover across list rows — popover never leaves the DOM, image swaps with no scale flicker, y trails with inertia; strip popover identical; grid card viewer thresholds unchanged in feel.

### Task 66: Pane intent gate + seen fast-path ✓ done

*2026-09-20 · feat(peek): 80ms intent, instant swap for seen media*

**Objective:** `attachWorkPeek` debounces hover swaps behind 80ms intent (fast travel starts no mosaic, pending cleared on same-slug and grid leave), swaps already-seen media instantly via the new `hasReveal` probe (`swapFast`, `.ready` held) while unseen media still re-mosaics (`swapReveal`), and warms pane imagery 800ms after mount; arch map regenerated for the new `gridReveal-load` edge.

**Files:** `src/views/masonry/work-peek.js`, `src/views/masonry/gridReveal-load.js`, `docs/graph.mmd`, `src/ds/arch-schema.json`

**Verify:** Build green; slow hover — mosaic ramp then `.ready`; fast sweep — no mosaic storm; revisit — instant swap, no flash; `npm run arch` shows no orphans.

*Shipped in 391dbf0 · Tasks 65–66 · phase-23.*

## Phase 24 — Fast list follow, rAF image swaps {#phase-24}

*Tags: Motion, Component*

*The list popover was lagging behind vertical travel — the Y spring was over-damped (0.07/0.8 + 14px dead zone) and image swaps were delayed 28ms. Retune for snap: YK 0.22/0.68 with 4px dead zone and K 0.14/0.64 for the grid viewer, swaps coalesced to one rAF (16ms), origin writes cached per element, and the strip/thumb image path uses fixed 240px + cover to avoid layout reflow.*

### Task 67: Retune follower + rAF swap ✓ done

*2026-09-20 · fix(hover): snappy Y follow, rAF-batched src swaps*

**Objective:** Bump `--fx-viewer-y-k` 0.07→0.22, `y-fr` 0.8→0.68, `y-dead` 14→4px and `k` 0.05→0.14 / `fr` 0.78→0.64 in `tokens-fx.css` (fallbacks in `strip-viewer-helpers.js` match); `attachListViewer` moves to a single grid `mousemove` + `requestAnimationFrame`-batched image swaps (no 28/80ms timeout), per-row `mousemove` removed, `decode()` warmup kept; `placeWithOrigin` caches `transformOrigin` per element via `WeakMap`; `preloadHeroes` also warms `decode()`.

**Files:** `src/styles/tokens-fx.css`, `src/views/strip-viewer-helpers.js`, `src/views/masonry/list-viewer.js`

**Verify:** Build green; fast up/down across list — popover Y catches within 1–2 frames, no dead-zone stick, image never more than one frame behind; strip popover identical; `lint-manage` passes for these files.

### Task 68: Stable popover geometry ✓ done

*2026-09-20 · fix(viewer): fixed 240px strip, cover crop, compositor hints*

**Objective:** `viewer.css` — `.viewer--strip` becomes `height:240px; contain:layout paint; backface-visibility:hidden` and `.viewer--strip img` is `height:100%; object-fit:cover`, `.viewer img` gets `translateZ(0)` so height no longer reflows on src swap and the follower animates on the compositor.

**Files:** `src/styles/viewer.css`

**Verify:** Build green; list popover stays 180×240 regardless of aspect, no height jump on row change, cover crop holds.

*Shipped in 76ab5ed · Tasks 67–68 · phase-24.*

## Phase 25 — Instant list follow {#phase-25}

*Tags: Motion, Component*

*List popover still felt a beat behind — the Y spring (even retuned) added 1–3 frames of lag and image decodes competed with the position tick. Make the list follow 1:1.*

### Task 69: Direct position, scale-only spring ✓ done

*2026-09-20 · fix(hover): list popover follows cursor without Y spring*

**Objective:** Add `createListFollower` in `strip-viewer-helpers.js` that writes `translate3d(x,y,0) scale(s)` with x/y set synchronously on `aim()`/`snap()` and only the pop scale (`FROM 0.985→1`) springs via `requestAnimationFrame`; `attachListViewer` switches from `createFollower` to `createListFollower` so vertical travel is instant, no dead zone, no Y inertia.

**Files:** `src/views/strip-viewer-helpers.js`, `src/views/masonry/list-viewer.js`

**Verify:** Build green; scrub fast up/down — popover stays glued to cursor (0 frames lag), scale pop still runs on first show; `lint-manage` 100-line budget holds.

*Shipped in 14b6143 · Task 69 · phase-25.*

## Phase 26 — Promo video admin pattern fix {#phase-26}

*Tags: Tooling*

*Admin rejected .mp4 uploads — double-escaped `pattern` left Decap expecting a literal `\` before the dot.*

### Task 70: Fix promoVideo file pattern ✓ done

*2026-09-20 · fix(admin): single-escaped `\.(mp4|webm|mov)$` so Decap accepts uploads*

**Objective:** Change `public/admin/config.yml` `promoVideo` `pattern` from `['\\.mp4$|\\.webm$|\\.mov$']` (double-escaped → `/\\.mp4$/` requiring leading `\`) to `['\.(mp4|webm|mov)$']` so `/videos/*.mp4` passes and jpg still fails; rebuild to sync `dist/admin`.

**Files:** `public/admin/config.yml`

**Verify:** `node` `new RegExp('\.(mp4|webm|mov)$').test('/videos/x.mp4')===true`, `jpg===false`; `npm test` green; `/admin/#/collections/projects` → Choose file → no red pattern error.

*Shipped in d6a38df · Task 70 · phase-26.*

## Phase 27 — Hero 60% + promo stacked in media column · video relocates to public {#phase-27}

*Tags: Layout, Component*

*Promo rendered full-width below the grid and the admin upload landed in `content/projects/public/videos` so the src 404'd and the dummy played; hero at 50% felt tight.*

### Task 71: Move misplaced video to public/videos ✓ done

*2026-09-20 · fix(cms): move `content/projects/public/videos/...mp4` → `public/videos/...mp4` so `/videos/...` resolves*

**Objective:** `git mv` the Decap-misplaced binary from `content/projects/public/videos` to `public/videos` (remove empty `content/projects/public`), keep `content/projects/tas-51.json` `promoVideo: "/videos/..."` unchanged — now the file is served at `/videos/...` and the dummy fallback no longer triggers.

**Files:** `public/videos/woman_standing_in_concrete_atrium_20260920203104.mp4`, `content/projects/public/videos/...` (deleted)

**Verify:** `ls public/videos` shows 2.7M mp4, `ls content/projects/public` empty, page `/projects/tas-51` video src is `/videos/...` and loads (no DUMMY).

### Task 72: Fix projects collection media_folder so future uploads land in public ✓ done

*2026-09-20 · fix(admin): remove `media_folder: ""` / `public_folder: ""` from Projects collection*

**Objective:** Delete the two empty overrides at `public/admin/config.yml` `projects` collection so field-level `promoVideo: {media_folder: "public/videos", public_folder: "/videos"}` resolves from repo root, not `content/projects/public/videos`; future “Choose different file” writes to `public/videos`.

**Files:** `public/admin/config.yml`

**Verify:** `grep -c 'media_folder: ""' public/admin/config.yml` → 0; re-upload test in `/admin` writes to `public/videos`.

### Task 73: Stack promo video inside the hero column ✓ done

*2026-09-20 · feat(case): promo video lives in the same column as the hero image*

**Objective:** Wrap hero and promo in one `.case-media` column: `project.js` renders `<div class="case-media"><div class="hero-box" …>…</div>${promo}</div>` (was `<div class="case-media hero-box">…</div></div>${promo}`); `contact.js` mirrors wrapper; `case.css` `hero-box` selector decoupled from `.case-media`, `case-media` becomes `flex column gap var(--space-14)`, `.case-media .case-promo {margin:0}` overrides.

**Files:** `src/views/project.js`, `src/views/contact.js`, `src/styles/case.css`, `src/styles/promo-video.css`

**Verify:** Desktop `tas-51` → hero image + 16:9 player stacked vertically in the right column (same 60% width), gap 14, no full-width bleed.

### Task 74: Hero everywhere 40/60 not 50/50 ✓ done

*2026-09-20 · style(case): hero column 60% of viewport, copy 40%*

**Objective:** Change `case.css` `.case-grid` from `1fr 1fr` (50/50) to `minmax(0,2fr) minmax(0,3fr)` (40/60) so hero (project + contact profile) occupies 60% of the available width on desktop; keep `@media 800px` stacked. Contact uses same grid, so its portrait also 60%.

**Files:** `src/styles/case.css`

**Verify:** Measure `case-grid` columns at >800px → right column ~60% width, left ~40%; `<800px` stacks single column.

*Shipped in f6d6614 · Tasks 71–74 · phase-27.*

## Phase 28 — Changelog coverage sweep: Sep 6–20 retro-linked {#phase-28}

*Tags: Tooling*

*Coverage audit found 99 commits (2026-09-06 → 09-20) that `changelog-manifest.json` never saw — pre-SOP trailer-less work, plan-only close/stamp chores, plumbing-excluded track refreshes, and portfolio/CMS commits the DS-path log never reads. All are now retro-mapped; the phaseless hero-load plan gained its retro phase so its commits have a visible home.*

| # | Task | Done when |
|---|------|-----------|
| 75 | Retro-link the 99 unseen shas | audit (git log vs manifest) reads 0 missing; each anchor verbatim in exactly one phase body |

### Task 75: Retro-link the 99 unseen shas ✓ done

*2026-09-21 · chore(ds): coverage sweep — 99 unseen commits retro-mapped*

**Objective:** Write one `.hermes/plan-links.json` entry per missing sha — trailer parsed where present, plan+phase resolved by content otherwise — every anchor validated verbatim in exactly one normalized phase body, then run `ds:track` so badges, the calendar heat graph and the unlinked triage all carry the full Sep 6–20 history.

**Files:** `.hermes/plan-links.json`, `.hermes/plans/2026-09-08_203300-hero-image-load-sequence.md`, `src/ds/changelog-manifest.json`

**Verify:** audit script (shas in `git log` absent from `manifest.commits`) prints "all tracked"; `npm run ds:track` reports 0 wip; `plan:names` green; `/ds/#/changelog` shows badges on lump-sum phases 14–27 and on the retro phases (hero-load, decap, mobile, masonry).

*Shipped in <pending> · Task 75 · phase-28.*



