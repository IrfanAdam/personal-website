# Lump-sum builds — shipments with no scoped plan

> **For Hermes:** the standing catch-all. A shipment no scoped plan covers appends one `### Task N: …` under the phase it belongs to (or opens a new phase) and closes with `*Shipped in <sha> · Tasks a–b · phase-N.*`.

**Goal:** Nothing ships untraceable — every push lands on a scoped plan or here, with its sha recorded next to what changed.

**Architecture:** One running plan file; each phase batches related shipments and each task records what changed plus the sha that proves it, so `git log`, `.hermes/plan-links.json` and `/ds/#/changelog` agree with each other.

**Tech Stack:** Vanilla JS + Vite, portfolio surface only (`main.js`, `src/views`, `src/styles`, `index.html`). Work inside `src/ds` gets a scoped plan of its own.

**Tags:** Layout, Motion, Component, Tooling

---

## Phase 1 — Post-ship mobile + media fixes {#phase-1}

*Tags: Layout*

*Five follow-up commits landed after the mobile-optimisation build closed, with no plan to carry them.*

### Task 1: Gear sits at the end of the icon row ✓ done

**Objective:** Stop the settings gear being fixed while the thumbnail strip scrolls.
**Files:** `src/main.js`, `src/styles/base.css`
**Verify:** Mobile — scrolling the strip leaves the gear in flow at the row end.

### Task 2: Full-bleed rows without the padding clip ✓ done

**Objective:** Remove the left/right padding that clipped full-bleed rows.
**Files:** `src/styles/masonry.css`
**Verify:** Grid rows span edge-to-edge.

### Task 3: Restore the grid gutter, keep the header full-bleed ✓ done

**Objective:** Grid keeps its left/right gutter while the header stays full-bleed.
**Files:** `src/styles/masonry.css`
**Verify:** Gutter visible both sides; header unchanged.

### Task 4: Mobile wrap gutter 16→12 ✓ done

**Objective:** Match the page gutter to the grid gap at ≤640px.
**Files:** `src/styles/masonry.css`
**Verify:** Card gap and page gutter read as one measure.

### Task 5: Responsive images + reveal flicker ✓ done

**Objective:** webp/jpeg srcsets, decode-await, smooth crossfade — kill the mobile reveal flicker.
**Files:** `src/views/img-helpers.js`, `src/views/masonry/cards.js`, `src/views/project.js`
**Verify:** No flash of un-decoded art on mobile load; `npm run smoke` green.

*Shipped in 96f28d6 · Tasks 1–5 · phase-1 — 0c80ab8, e494169, 8e440b7, a330be7 precede it.*

## Phase 2 — Header snap + title pace {#phase-2}

*Tags: Motion*

### Task 6: Strip icon press snap ✓ done

**Objective:** Clicking a strip icon (project thumbs, contact, list/grid) plays the `load.wav` snap through a new `click` sound slot; the loading bed stays `loading.mp3`.
**Files:** `src/views/strip-sound.js`, `src/views/sound-source.js`, `src/views/slot-sound.js`
**Verify:** Every strip icon snaps on click; verifies with sound on in a real browser.

### Task 7: Title scramble 200ms, first visit only ✓ done

**Objective:** Page-title scramble 900ms → 200ms and only on the first visit to a route per session; repeat visits render instantly with no loading sound.
**Files:** `src/views/title-reveal.js`, `src/views/init-sound-load.js`
**Verify:** Measured 212ms scramble on first visit; second visit produced zero scramble frames.

### Task 8: Map regenerated for the committed tree ✓ done

**Objective:** `docs/graph.mmd` had captured nodes from a working tree holding uncommitted modules; regenerate against committed source.
**Files:** `docs/graph.mmd`
**Verify:** Clean checkout — `node scripts/map-graph.mjs` leaves the file unmodified.

*Shipped in 05ec5b2 · Tasks 6–7 · phase-2 — map fix in 8d0fdc5.*

## Phase 3 — Ship rule: no untraceable push {#phase-3}

*Tags: Tooling*

### Task 9: Name + link this lump sum ✓ done

**Objective:** Give this plan its `changelog-names.json` entry and map the seven plan-less shas to their phases in `.hermes/plan-links.json`.
**Files:** `src/ds/changelog-names.json`, `.hermes/plan-links.json`
**Verify:** `npm run plan:names` reports 0 missing; `npm run ds:track` reports tagged = phased.

### Task 10: Standing ship rule in the repo docs ✓ done

**Objective:** `.cursorrules` §5, `AGENTS.md` §5 and README "DS conventions" state the rule — no push without a plan: reuse the covering plan's trailer, else append a task here.
**Files:** `.cursorrules`, `AGENTS.md`, `README.md`
**Verify:** The three files state the same rule; push-time report lists pushed commits and leftover wip.

### Task 11: Retro-link the atlas phase-4.5 planning commit ✓ done

**Objective:** `b5b29ce` cited `#phase-4-5`, an anchor no phase body carries, so it sat in "Unlinked DS changes" — re-point it at the atlas build's phase-5 (dock polish) via `.hermes/plan-links.json`.
**Files:** `.hermes/plan-links.json`, `src/ds/changelog-manifest.json`
**Verify:** `npm run ds:track` → unlinked 0; `/ds/#/changelog` reads "All tracked — every DS commit cites its plan".

### Task 12: Deployed archive reads clean ✓ done

**Objective:** The deployed changelog showed two phantoms — an unlinked plan-mark chore (`2e1b95b`) and `M src/ds/arch-schema.json` under "Uncommitted". Retro-link the chore to the mobile build's phase-4, and stop ds-track reporting the two build-generated DS files (manifest, regenerated schema) as wip.
**Files:** `scripts/ds-track.mjs`, `.hermes/plan-links.json`
**Verify:** Retro links apply after the prev-merge (a merged entry gets linked too); the deployed `/ds/#/changelog` shows no unlinked or uncommitted lines.

### Task 13: Runtime DB stays out of git ✓ done

**Objective:** `.hermes/sessions.db` (empty Hermes runtime file) showed as untracked noise in every status; ignore it like the other local artifacts.
**Files:** `.gitignore`
**Verify:** `git status --short` lists no `.hermes/sessions.db`.

### Task 14: Tracker stops dirtying the tree ✓ done

**Objective:** `npm run dev`/`build` rewrote the manifest with a fresh timestamp even when nothing changed, so `git status` always showed `M src/ds/changelog-manifest.json` — read as "uncommitted code". ds-track now keeps the previous `generated` stamp when `continuation/plans/commits/wip` are unchanged, so an unchanged run leaves the file byte-identical.
**Files:** `scripts/ds-track.mjs`
**Verify:** two consecutive `npm run ds:track` runs → identical checksum; `git status --short` clean.

### Task 15: Generated DS files never dirty the tree ✓ done

**Objective:** `docs/graph.mmd` regenerates regardless, but the two generated DS files trailed every build and every commit: `arch-schema.json` restamped `generated` on each `map-graph` run, and a manifest-only refresh commit re-entered the log, so the manifest could never match its committed self. Both now behave: the schema keeps its stamp when `nodes`/`edges` are unchanged, and a commit whose only DS changes are the two generated files is treated as plumbing and excluded from the log.
**Files:** `scripts/ds-track.mjs`, `scripts/map-graph.mjs`
**Verify:** `npm run ds:track` + `node scripts/map-graph.mjs` twice in a row → identical checksums and a clean `git status`.

*Shipped in 9a82f0f · Tasks 9–15 · phase-3.*

## Phase 4 — Sound polish: hero + reel + header slot {#phase-4}

*Tags: Motion*

### Task 16: Hero window-open on desktop ✓ done

**Objective:** Every hero mosaic plays `window-open` at 0.12 on desktop too.
**Files:** `src/views/project.js`, `src/views/sound-source.js`
**Verify:** Desktop hero reveal emits window-open; mobile unchanged.

### Task 17: Desktop hero de-overlap + second-load silence ✓ done

**Objective:** Loading bed and window-open no longer overlap; second visit to same hero is silent.
**Files:** `src/views/init-sound-load.js`, `src/views/project.js`
**Verify:** No double sound on first load; revisit is silent.

### Task 18: Hero window-open 620→480ms ✓ done

**Objective:** Tighten desktop hero coupling from 620ms to 480ms.
**Files:** `src/views/project.js`
**Verify:** Window-open fires 480ms after grid reveal completes.

### Task 19: Mobile reel linger ✓ done

**Objective:** Card highlight pulls 0.34 with linger; grid reveal stays silent on reel.
**Files:** `src/views/masonry/cards.js`, `src/views/slot-sound.js`
**Verify:** Reel highlight scrambles with pull 0.34; auto grid reveal is silent.

### Task 20: Master volume × per-sound gain ✓ done

**Objective:** Master volume slider drives per-sound gain; faint visitor default, mobile scramble lingers correctly.
**Files:** `src/views/init-sound.js`, `src/views/slot-sound.js`, `src/views/sound-source.js`
**Verify:** Slider attenuates all slots; defaults are faint but audible.

### Task 21: Header sound outside gear pop ✓ done

**Objective:** Sound toggle lives outside the gear dropdown — visible on desktop, dropdown not clipped.
**Files:** `src/main.js`, `src/views/init-sound.js`
**Verify:** Desktop shows sound switch beside strip; gear pop no longer clips.

### Task 22: Header slot fix — soundSwitch to #soundSlot ✓ done

**Objective:** Follow-up to Task 21 — move switch insertion to `#soundSlot`, clean legacy header-inserted switches.
**Files:** `src/main.js`, `src/views/init-sound.js`
**Verify:** Switch renders in `#soundSlot` only; direct-header `.sound-switch` nodes are removed on ensure.

*Shipped in b162ab6 · Tasks 16–22 · phase-4.*

## Phase 5 — Reel shutter: interruptible zing {#phase-5}

*Tags: Motion*

### Task 23: Zing reel stops previous, 45ms shutter ✓ done

**Objective:** Mobile card highlight `zing.mp3` was gated at 380ms and queued behind the file — fast scroll sounded erratic. New `reel-tick.js` kills the active `Audio` before firing next, throttle 380→45ms, so each card ticks and previous is cut like a shutter reel.
**Files:** `src/views/masonry/reel-tick.js`, `src/views/masonry/reveal.js`, `docs/graph.mmd`, `src/ds/arch-schema.json`
**Verify:** Fast scroll on ≤640px emits a tick per card with previous cut; slow scroll still ticks once per highlight; dev server `http://192.168.0.104:5173` audible.

*Shipped in e2252fd · Tasks 23–23 · phase-5.*

## Phase 6 — Loading sound sync + hero height cache {#phase-6}

*Tags: Motion*

*One commit pairs the loading bed with the first scramble and caches hero height per tab.*

| # | Task | Done when |
|---|------|-----------|
| 24 | Loading sound follows first scramble, hero height cached | Loading bed fires with first scramble; hero height cached per tab |

### Task 24: Loading sound follows first scramble, hero height cached ✓ done

*2026-09-15 19:35 · 08d6e66 · feat(portfolio): sync loading sound to first scramble and cache hero height per tab*

**Objective:** Loading bed fires with the first title scramble, not before; hero height cached per tab so revisit lays out instantly.
**Files:** `src/views/init-sound-load.js`, `src/views/hero-height-cache.js`, `src/views/title-reveal.js`, `src/views/rise.js`
**Verify:** First visit plays the loading bed with the scramble; revisit is silent with hero height intact.

*Shipped in 08d6e66 · Tasks 24–24 · phase-6.*

## Phase 7 — Settings surface: sheet, modal, anchored dropdown {#phase-7}

*Tags: Component*

*Nine commits hunting the settings home — footer morph, bottom sheet, centered modal, anchored dropdown.*

| # | Task | Done when |
|---|------|-----------|
| 25 | Modern footer, mobile settings + morphing close | Footer modern; morphing close on mobile |
| 26 | Restore settings sheet, hero title to footer | Sheet restored; title in footer |
| 27 | Pill-height settings, dropdown anchored under gear | Control matches pill; dropdown under gear |
| 28 | Gear matches pill height | Heights align exactly |
| 29 | Bottom sheet back, near gear on mobile list | Sheet near gear on mobile |
| 30 | Dropdown via fWrap absolute, escapes backdrop-filter | Unclipped over header |
| 31 | Settings as centered modal + backdrop overlay | Centered modal with scrim |
| 32 | Mobile-only modal, full-width volume, keep frame | Mobile volume spans modal |
| 33 | Outside-close on desktop, volume full-width in mobile modal | Outside click closes; mobile volume full-width |

### Task 25: Modern footer, mobile settings + morphing close ✓ done

*2026-09-16 12:41 · 031395c · feat(portfolio): modern footer, mobile settings + morphing close*

**Objective:** Footer goes modern; mobile settings gain a morphing close control.
**Files:** `src/main.js`, `src/views/stripbar.js`, `src/views/settings-menu.js`, `src/styles/pages.css`
**Verify:** Footer renders modern; mobile settings open and morph closed.

### Task 26: Restore settings sheet, hero title to footer ✓ done

*2026-09-16 12:54 · 3299e52 · fix(portfolio): restore settings sheet + move hero title to footer*

**Objective:** Bring back the settings sheet; hero title moves down to the footer.
**Files:** `src/views/masonry.js`, `src/styles/pages.css`
**Verify:** Sheet opens from gear; hero title sits in the footer.

### Task 27: Pill-height settings, dropdown anchored under gear ✓ done

*2026-09-16 12:58 · 6d9fdd1 · fix(portfolio): pill-height settings + anchored dropdown under gear*

**Objective:** Settings control matches pill height; dropdown anchors under the gear.
**Files:** `src/views/settings-menu.js`, `src/main.js`, `src/styles/base-actions.css`, `src/styles/pages.css`
**Verify:** Gear and pill share height; dropdown opens beneath the gear.

### Task 28: Gear matches pill height ✓ done

*2026-09-16 13:01 · bf62cf7 · fix(portfolio): gear matches pill height + dropdown anchored*

**Objective:** Follow-up alignment — gear height tracks the pill exactly.
**Files:** `src/styles/base-actions.css`, `src/styles/pages.css`
**Verify:** Gear and pill heights match at all widths.

### Task 29: Bottom sheet back, near gear on mobile list ✓ done

*2026-09-16 13:06 · 6ed4848 · fix(portfolio): restore bottom sheet + position near gear on mobile list*

**Objective:** Restore the bottom sheet; on mobile list it positions near the gear.
**Files:** `src/views/settings-menu.js`
**Verify:** Mobile list shows the sheet anchored near the gear.

### Task 30: Dropdown via fWrap absolute, escapes header backdrop-filter ✓ done

*2026-09-16 13:08 · 77e8757 · fix(portfolio): dropdown via fWrap absolute, escape header backdrop-filter*

**Objective:** Dropdown positions via fWrap absolute so the header backdrop-filter no longer clips it.
**Files:** `src/views/settings-menu.js`
**Verify:** Dropdown opens unclipped over the header filter.

### Task 31: Settings as centered modal + backdrop overlay ✓ done

*2026-09-16 13:10 · bf97af5 · feat(portfolio): settings as centered modal + backdrop overlay*

**Objective:** Settings become a centered modal with a backdrop overlay.
**Files:** `src/views/settings-menu.js`, `src/styles/base.css`, `src/styles/pages.css`
**Verify:** Settings open centered with dimmed backdrop; scrim closes.

### Task 32: Mobile-only modal, full-width volume, keep frame ✓ done

*2026-09-16 13:14 · 66c9ced · fix(portfolio): mobile modal only, full-width volume, no right pad, keep frame*

**Objective:** Modal treatment applies on mobile only; volume goes full-width with no right pad, frame kept.
**Files:** `src/views/settings-menu.js`, `src/views/init-sound-vol.js`, `src/styles/base.css`
**Verify:** Desktop unchanged; mobile modal shows full-width volume.

### Task 33: Outside-close on desktop, volume full-width in mobile modal ✓ done

*2026-09-16 13:18 · 73db667 · fix(portfolio): outside close desktop, volume in mobile modal full width*

**Objective:** Desktop modal closes on outside click; mobile modal keeps full-width volume.
**Files:** `src/views/settings-menu.js`, `src/views/init-sound.js`
**Verify:** Desktop outside click closes; mobile volume spans the modal.

*Shipped in 73db667 · Tasks 25–33 · phase-7 — 031395c, 3299e52, 6d9fdd1, bf62cf7, 6ed4848, 77e8757, bf97af5, 66c9ced precede it.*

## Phase 8 — Reel zing gating: first card to per-card ready {#phase-8}

*Tags: Motion*

*Four increasingly tight gates on the mobile grid zing so scrolling ticks only over revealed cards.*

| # | Task | Done when |
|---|------|-----------|
| 34 | Mobile grid zing waits for first card reveal | Silent until first reveal |
| 35 | Zing waits until all grid cards ready | Holds until full grid ready |
| 36 | Zing after first fold of 6 cards ready | Starts at 6-card fold |
| 37 | Per-card zing on card.ready plus first-fold | Ready cards tick past fold |

### Task 34: Mobile grid zing waits for first card reveal ✓ done

*2026-09-16 13:21 · 3225bf7 · fix(sound): gate mobile grid zing until first card reveal ready*

**Objective:** Grid zing stays silent until the first card reveal is ready.
**Files:** `src/views/masonry/reel-tick.js`
**Verify:** No zing before first reveal; ticks start after.

### Task 35: Zing waits until all grid cards ready ✓ done

*2026-09-16 13:30 · acb1bdd · fix(sound): gate zing until all grid cards ready*

**Objective:** Tighten the gate — zing holds until every grid card is ready.
**Files:** `src/views/masonry/reel-tick.js`
**Verify:** Early scroll is silent; full grid ticks.

### Task 36: Zing after first fold of 6 cards ready ✓ done

*2026-09-16 13:31 · 11e9955 · fix(sound): zing after first fold (6 cards) ready*

**Objective:** Relax to first-fold readiness — six cards suffice for the zing to start.
**Files:** `src/views/masonry/reel-tick.js`
**Verify:** Zing begins once the first fold is ready.

### Task 37: Per-card zing on card.ready plus first-fold ✓ done

*2026-09-16 13:34 · f91c68f · fix(sound): per-card zing gated on card.ready + first-fold*

**Objective:** Each card zings only when itself ready, and only past the first fold.
**Files:** `src/views/masonry/reel-tick.js`, `src/views/masonry/reveal.js`
**Verify:** Unready cards stay silent; ready cards tick past the fold.

*Shipped in f91c68f · Tasks 34–37 · phase-8 — 3225bf7, acb1bdd, 11e9955 precede it.*

## Phase 9 — Proceed gate: cyberpunk authorize, subtle redux {#phase-9}

*Tags: Component*

*Entry gate arrives loud then leaves quiet — cyberpunk authorize preview first, glimmer-orb redux after.*

| # | Task | Done when |
|---|------|-----------|
| 38 | Cyberpunk proceed gate + live authorize preview on volume seek | Gate with live preview |
| 39 | Subtle redux — glimmer orb + dotted corners | Orb glimmers; corners dotted |

### Task 38: Cyberpunk proceed gate + live authorize preview on volume seek ✓ done

*2026-09-16 13:42 · 05e9ab5 · feat(portfolio): cyberpunk proceed gate + live authorize preview on volume seek*

**Objective:** New proceed gate with live authorize preview that reacts to volume seek.
**Files:** `src/views/proceed-gate.js`, `src/styles/proceed.css`, `src/views/init-sound.js`, `index.html`
**Verify:** Gate authorizes with cyberpunk styling; seeking volume previews live.

### Task 39: Subtle redux — glimmer orb + dotted corners ✓ done

*2026-09-16 13:46 · e3db2c1 · feat(proceed): subtle redux gate with glimmer orb + dotted corners*

**Objective:** Tone the gate down — glimmer orb and dotted corners, less neon.
**Files:** `src/views/proceed-gate.js`, `src/styles/proceed.css`
**Verify:** Gate reads subtle; orb glimmers, corners dotted.

*Shipped in e3db2c1 · Tasks 38–39 · phase-9 — 05e9ab5 precedes it.*

## Phase 10 — Token + changelog: gate vars, heat & badge times {#phase-10}

*Tags: Tooling*

*The proceed gate shipped with raw px (18 violations) and the changelog's heat/badges hid portfolio work — this pass tokenizes the gate and teaches the tracker/calendar to show every retro-linked sha with `date time`.*

| # | Task | Done when |
|---|------|-----------|
| 40 | Gate and menu use token vars + changelog heat/badge times | `lint:tokens` clean; calendar heat + badges show `date time` for every linked sha |

### Task 40: Gate and menu use token vars, no raw px ✓ done

*2026-09-16 13:59 · a8199b3 · chore(lump-sum): phases 6-10 — retro 16 shas + gate token compliance*

**Objective:** Proceed gate, base menu pop, the tokens they need, and the changelog (calendar heat + per-task badges with time) all use var()/tracked commits; lint:tokens passes 38+33 clean.
**Files:** `src/styles/proceed.css`, `src/styles/base.css`, `src/styles/tokens-type.css`, `src/ds/changelog-links.js`, `scripts/ds-track.mjs`
**Verify:** `npm run lint:tokens` → ✓ clean; `npm test` → build succeeds; `node scripts/ds-track.mjs` → 222/222 linked · Sep 16 heat shows 10+ commits · badges show `date time`.

*Shipped in a8199b3 · Tasks 40–40 · phase-10.*
